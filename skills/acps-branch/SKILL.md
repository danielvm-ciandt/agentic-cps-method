---
name: acps-branch
description: 'Creates a semantic git branch for the current iteration story following CPS GSD discipline. Always run first — no loop work starts without a branch. Enforces the naming convention: {iteration}/{epic-id}-{story-slug}.'
---

Follow the instructions in ./workflow.md.
