---
name: acps-init
description: 'Initializes a project for CPS methodology. Creates the .planning/ directory structure (epics/, iterations/, specs/) and writes .acps-config.json with the planningDir key. Run once at project start before any other acps skill.'
---

Follow the instructions in ./workflow.md.
