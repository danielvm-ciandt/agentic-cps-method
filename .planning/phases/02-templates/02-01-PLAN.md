---
phase: 02-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/specs/epic.md
  - templates/specs/story.md
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02

must_haves:
  truths:
    - "templates/specs/epic.md exists with sections: Overview, Requirements checklist, Non-goals, Acceptance criteria"
    - "templates/specs/story.md exists with YAML front-matter containing sessions[] and totals block"
    - "story.md body follows lean-spec sections after the closing --- delimiter"
    - "story.md has no lean-spec lifecycle fields (status, transitions, priority) in front-matter"
    - "epic.md has no YAML front-matter (plain Markdown only)"
  artifacts:
    - path: "templates/specs/epic.md"
      provides: "Epic template with prescribed sections"
      contains: "## Requirements"
    - path: "templates/specs/story.md"
      provides: "Story template with YAML telemetry front-matter"
      contains: "sessions:"
  key_links:
    - from: "templates/specs/story.md"
      to: "acps-execute skill (Phase 3)"
      via: "sessions[] array append"
      pattern: "sessions:\\s*\\[\\]"
    - from: "templates/specs/story.md"
      to: "acps-homologate skill (Phase 3)"
      via: "totals block write"
      pattern: "totals:"
---

<objective>
Create the two spec templates: epic.md (lean-spec format with prescribed CPS sections) and story.md (YAML telemetry front-matter + lean-spec body).

Purpose: These templates are the canonical artifact definitions consumed by acps-new-epic and acps-new-story in Phase 3. Getting the schema right here prevents breaking changes later.

Output: templates/specs/epic.md, templates/specs/story.md
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/02-templates/02-CONTEXT.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/02-templates/02-RESEARCH.md

<interfaces>
<!-- Canonical lean-spec body structure (from /Users/danielvm/Sites/lean-spec/.lean-spec/templates/spec-template.md) -->
<!-- Executor MUST read the reference file before writing any template. -->

Sections (in order): Overview, Design, Plan, Test, Notes
Placeholder style: HTML comments <!-- text -->
Front-matter: YAML 1.1 --- delimiters (lean-spec uses it; acps only uses it for story.md)

story.md YAML schema (locked: D-01, D-02, D-03 from CONTEXT.md):
```yaml
---
story_id: ""
sessions: []
totals:
  sessions_count: 0
  total_duration_ms: 0
  tokens:
    input: 0
    output: 0
    total: 0
---
```

epic.md sections (locked: TMPL-01 from REQUIREMENTS.md):
  Overview, Requirements (checklist), Non-goals, Acceptance Criteria
  NOTE: This deviates from generic lean-spec sections — use these prescribed headings.
  NOTE: No YAML front-matter on epic.md (D-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create templates/specs/epic.md</name>
  <files>templates/specs/epic.md</files>
  <read_first>
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/02-templates/02-CONTEXT.md (decisions D-04, D-07)
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md (TMPL-01 section prescription)
    - /Users/danielvm/Sites/lean-spec/.lean-spec/templates/spec-template.md (body style to model)
  </read_first>
  <action>
Create `templates/specs/epic.md` as a plain Markdown file (NO YAML front-matter — D-04).

Use the sections prescribed by TMPL-01:
1. `# {epic_title}` — H1 title placeholder
2. Status line: `> **Status**: planned`
3. `## Overview` — with comment `<!-- What is this epic solving? Business context and value. -->`
4. `## Requirements` — with a checklist of two placeholder items: `- [ ] Requirement 1` and `- [ ] Requirement 2`
5. `## Non-goals` — with comment `<!-- What this epic explicitly does NOT do -->`
6. `## Acceptance Criteria` — with two placeholder checklist items: `- [ ] AC 1` and `- [ ] AC 2`

No YAML front-matter. No emoji. No bracket placeholders — HTML comments only for prose sections.
The Requirements and Acceptance Criteria sections use Markdown checklist items (not comments) as placeholder rows.
  </action>
  <verify>
    <automated>grep -n "## Requirements" /Users/danielvm/Sites/agentic-cps-method/templates/specs/epic.md && grep -n "## Non-goals" /Users/danielvm/Sites/agentic-cps-method/templates/specs/epic.md && grep -n "## Acceptance Criteria" /Users/danielvm/Sites/agentic-cps-method/templates/specs/epic.md && ! grep -n "^---" /Users/danielvm/Sites/agentic-cps-method/templates/specs/epic.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at templates/specs/epic.md
    - grep "## Requirements" templates/specs/epic.md exits 0
    - grep "## Non-goals" templates/specs/epic.md exits 0
    - grep "## Acceptance Criteria" templates/specs/epic.md exits 0
    - grep "## Overview" templates/specs/epic.md exits 0
    - File does NOT contain a line matching ^--- (no YAML front-matter)
    - File does NOT contain the word "Design" as an H2 heading
    - File does NOT contain the word "Test" as an H2 heading
    - File does NOT contain emoji characters
  </acceptance_criteria>
  <done>epic.md exists with the four prescribed H2 sections, no YAML front-matter, no emoji, HTML comments for prose placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Create templates/specs/story.md</name>
  <files>templates/specs/story.md</files>
  <read_first>
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/02-templates/02-CONTEXT.md (decisions D-01, D-02, D-03, D-05)
    - /Users/danielvm/Sites/lean-spec/.lean-spec/templates/spec-template.md (body section structure)
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md (TMPL-02)
  </read_first>
  <action>
Create `templates/specs/story.md` with YAML front-matter followed by a lean-spec body.

Part 1 — YAML front-matter (exactly as specified in D-01/D-02/D-03, no additions):
```
---
story_id: ""
sessions: []
totals:
  sessions_count: 0
  total_duration_ms: 0
  tokens:
    input: 0
    output: 0
    total: 0
---
```

CRITICAL rules for YAML:
- `sessions:` MUST be `sessions: []` (empty array literal) — NOT `sessions: null`, NOT omitted
- `tokens` in `totals` MUST use proper nested YAML (indented sub-keys), NOT dot notation
- Do NOT add lean-spec lifecycle fields: no `status:`, no `transitions:`, no `priority:`, no `created:`, no `tags:`
- `story_id: ""` is a plain string key — include it as the first key before `sessions:`

Part 2 — lean-spec body (D-05 — all templates including story.md follow lean-spec sections):
After the closing `---`, write:

```
# {story_title}

> **Status**: planned

## Overview

<!-- Describe what this story delivers and why -->

## Design

<!-- Technical approach and decisions -->

## Plan

- [ ] Task 1
- [ ] Task 2

## Test

- [ ] Acceptance criteria 1
- [ ] Acceptance criteria 2

## Notes

<!-- Research findings, references, open questions -->
```

No emoji. No bracket placeholders in prose sections — HTML comments only.
  </action>
  <verify>
    <automated>grep -n "^sessions: \[\]" /Users/danielvm/Sites/agentic-cps-method/templates/specs/story.md && grep -n "^totals:" /Users/danielvm/Sites/agentic-cps-method/templates/specs/story.md && grep -n "^  sessions_count: 0" /Users/danielvm/Sites/agentic-cps-method/templates/specs/story.md && grep -n "## Overview" /Users/danielvm/Sites/agentic-cps-method/templates/specs/story.md && grep -n "## Design" /Users/danielvm/Sites/agentic-cps-method/templates/specs/story.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at templates/specs/story.md
    - grep "^sessions: \[\]" templates/specs/story.md exits 0 (empty array, not null)
    - grep "^totals:" templates/specs/story.md exits 0
    - grep "^  sessions_count: 0" templates/specs/story.md exits 0
    - grep "^  total_duration_ms: 0" templates/specs/story.md exits 0
    - grep "## Overview" templates/specs/story.md exits 0
    - grep "## Design" templates/specs/story.md exits 0
    - grep "## Plan" templates/specs/story.md exits 0
    - grep "## Test" templates/specs/story.md exits 0
    - File does NOT contain "transitions:" (no lean-spec lifecycle fields)
    - File does NOT contain "priority:" (no lean-spec lifecycle fields)
    - File does NOT contain "tokens.input" as a flat key (must use nested YAML)
    - grep "tokens:" templates/specs/story.md exits 0 (nested key exists)
  </acceptance_criteria>
  <done>story.md exists with correct YAML front-matter (sessions:[] empty array, totals block with nested tokens) and lean-spec body sections Overview/Design/Plan/Test/Notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static Markdown file authoring — no user input, no network, no runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | story.md YAML schema | accept | Template is static content; schema integrity verified by acceptance criteria grep checks |
</threat_model>

<verification>
After both tasks complete:

```bash
ls /Users/danielvm/Sites/agentic-cps-method/templates/specs/
# Expected: epic.md  story.md

grep "## Requirements" /Users/danielvm/Sites/agentic-cps-method/templates/specs/epic.md
grep "## Non-goals" /Users/danielvm/Sites/agentic-cps-method/templates/specs/epic.md
grep "sessions: \[\]" /Users/danielvm/Sites/agentic-cps-method/templates/specs/story.md
grep "totals:" /Users/danielvm/Sites/agentic-cps-method/templates/specs/story.md
```
</verification>

<success_criteria>
- templates/specs/ exists with exactly two files: epic.md and story.md
- epic.md: no front-matter, sections are Overview / Requirements / Non-goals / Acceptance Criteria
- story.md: YAML front-matter with story_id, sessions:[], totals block; body has Overview/Design/Plan/Test/Notes
- Neither file contains emoji
- No flat dot-notation YAML keys in story.md
</success_criteria>

<output>
After completion, create `.planning/phases/02-templates/02-01-SUMMARY.md` using the summary template.
</output>
