---
phase: 01-foundation-and-docs
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified:
  - README.md
autonomous: true
requirements:
  - FOUND-05

must_haves:
  truths:
    - "A first-time GitHub visitor understands what acps is within 30 seconds"
    - "Install command is clearly visible: npx acps@latest"
    - "README Requirements section lists only Node.js >= 24 (no pnpm)"
    - "README links to docs/getting-started.md"
  artifacts:
    - path: "README.md"
      provides: "Project entry point — methodology overview, install, quick start, docs links"
      contains: "npx acps@latest"
  key_links:
    - from: "README.md"
      to: "docs/getting-started.md"
      via: "Documentation section link"
      pattern: "getting-started"
    - from: "README.md"
      to: "docs/"
      via: "Documentation section links"
      pattern: "docs/"
---

<objective>
Write a fresh README.md following the kickass-readme standard (D-09). Do NOT copy from old repo. Target audience: someone who found this repo on GitHub for the first time.

Purpose: README is the project's public face. It must answer "what is this, why does it exist, and how do I start?" in one scroll.

Output: README.md at repo root
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/method/vision.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/01-foundation-and-docs/01-CONTEXT.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/01-foundation-and-docs/01-RESEARCH.md
@/Users/danielvm/Sites/agentic-cps-method/docs/standards/readme.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write fresh README.md</name>
  <files>README.md</files>
  <read_first>
    - /Users/danielvm/Sites/agentic-cps-method/.planning/method/vision.md (for accurate problem/solution framing — the "why it exists" content)
    - /Users/danielvm/Sites/agentic-cps-method/docs/standards/readme.md (kickass-readme standard — structure to follow)
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/01-foundation-and-docs/01-CONTEXT.md (D-09: fresh README, kickass-readme structure, no pnpm, target first-time GitHub visitor)
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/01-foundation-and-docs/01-RESEARCH.md (README structure example in "Code Examples" section)
    - /Users/danielvm/Sites/old-agentic-cps-method/README.md (read to understand what NOT to copy — old repo README is tied to the old history)
  </read_first>
  <action>
Write a fresh `README.md` at the repo root. Do NOT copy from the old repo. Follow the kickass-readme standard from `docs/standards/readme.md`.

Required sections and content:

**Title and one-liner:**
```markdown
# acps

CPS methodology discipline as installable AI IDE skills.
```

**Why it exists section** (2-4 sentences drawn from vision.md):
- AI-powered teams lack a methodology that speaks CPS language (phases, iterations, epics, homologation)
- Existing SDD tools (GSD, BMAD, lean-spec) are either too lightweight or too generic for enterprise CPS discipline
- acps bridges this gap: filtered CPS practices delivered as skills that feel like GSD commands

**Install section:**
```bash
npx acps@latest
```
Add a note that the installer prompts for: name, language, runtime (Claude Code, Cursor, Windsurf, and 7 others), location (global/local), and estimation method (BCP Full, BCP Simplified, FP+SNAP).

**Quick start section (5 commands):**
```text
/acps-help      → methodology overview + recommended next command
/acps-init      → create .planning/ in your project
/acps-branch    → create semantic branch (always first in the loop)
/acps-discuss   → extract implementation decisions
/acps-spec      → write acceptance criteria (ambiguity gate ≤ 0.20)
```

**The GSD loop section** (brief, scannable):
```text
acps-branch → acps-discuss → acps-spec → acps-plan → acps-execute → acps-homologate
```
One sentence: "Branch first, always. Spec gates block progress if ambiguity > 0.20."

**Documentation section with links:**
- [Getting started](docs/getting-started.md)
- [Phases](docs/phases/setup.md)
- [Workflow steps](docs/workflow/discuss.md)
- [Phase gates](docs/gates.md)
- [Standards](docs/standards/conventional-commits.md)

**Requirements section:**
```markdown
## Requirements

- Node.js >= 24
```
DO NOT add pnpm, npm, or any package manager to Requirements. End users run `npx acps@latest` — they do not need npm directly.

**License line at bottom:**
MIT

Style guidelines:
- Keep it scannable — no walls of text
- Max 2 sentences per prose paragraph
- Use code blocks for commands, not inline code spans
- No tables of features (save those for docs/)
- No badges in v1 (CI is not set up until first commit to main)
  </action>
  <verify>
    <automated>grep -q "npx acps@latest" /Users/danielvm/Sites/agentic-cps-method/README.md && grep -q "getting-started" /Users/danielvm/Sites/agentic-cps-method/README.md && ! grep -qi "pnpm\|corepack" /Users/danielvm/Sites/agentic-cps-method/README.md && grep -q "Node.js >= 24" /Users/danielvm/Sites/agentic-cps-method/README.md && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root
    - Contains `npx acps@latest` install command
    - Contains link to `docs/getting-started.md`
    - Contains `## Requirements` section with only `Node.js >= 24`
    - Does NOT contain `pnpm` anywhere
    - Does NOT contain `corepack` anywhere
    - Contains `/acps-help`, `/acps-init`, `/acps-branch` in quick start section
    - Contains the GSD loop sequence: branch → discuss → spec → plan → execute → homologate
    - File is non-trivially long (more than 20 lines — not a stub)
  </acceptance_criteria>
  <done>README.md written fresh following kickass-readme standard; install command, quick start, docs links, and Node.js requirement all present; no pnpm references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → GitHub renders | Public-facing content; must not contain misleading instructions or harmful links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | README.md content | accept | README is intentionally public. No secrets, credentials, or internal paths. |
| T-05-02 | Tampering | npx acps@latest install command | accept | Package name `acps` is what the package.json declares. No squatting risk for a new package with no npm publish yet. |
</threat_model>

<verification>
After task completes:

1. `README.md` exists and is non-empty
2. `grep "npx acps@latest" README.md` returns a match
3. `grep "pnpm\|corepack" README.md` returns no matches
4. `grep "getting-started" README.md` returns a match (docs link present)
5. `wc -l README.md` returns > 20 lines (not a stub)
6. `grep "Node.js >= 24" README.md` confirms only Node.js in Requirements
</verification>

<success_criteria>
- README.md exists and accurately describes the project to a first-time visitor
- Install command is `npx acps@latest`
- Quick start shows the 5+ key commands
- Documentation section links to existing docs/ files
- Requirements lists only Node.js >= 24
- Zero pnpm/corepack references
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-docs/01-05-SUMMARY.md`
</output>
