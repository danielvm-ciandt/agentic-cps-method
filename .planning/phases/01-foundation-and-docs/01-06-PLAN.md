---
phase: 01-foundation-and-docs
plan: 06
type: execute
wave: 5
depends_on:
  - 01-02
  - 01-05
files_modified: []
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06

must_haves:
  truths:
    - "npm install succeeds from a clean state"
    - "npm run lint passes with exit code 0 across entire docs/ tree and README.md"
    - "node --check bin/install.js exits 0"
    - "node bin/install.js prints the stub message"
    - "All 24 docs files exist in the correct paths"
    - "grep -r pnpm docs/ returns no matches"
  artifacts:
    - path: "package-lock.json"
      provides: "Deterministic lockfile for npm ci"
      contains: "acps"
    - path: "node_modules/"
      provides: "Installed devDependencies"
      contains: "markdownlint-cli2"
  key_links:
    - from: "npm run lint"
      to: "docs/**/*.md"
      via: "markdownlint-cli2 glob"
      pattern: "docs/"
    - from: "npm run lint"
      to: "README.md"
      via: "*.md glob"
      pattern: "README.md"
---

<objective>
Run end-to-end verification: npm install, lint check, and node --check. Then perform a human checkpoint to confirm the full docs tree is correct before the phase is marked complete.

Purpose: Catches any issues with the assembled files before the phase is closed — lint failures, missing docs, or leftover pnpm references in docs.

Output: package-lock.json (created by npm install); human verification sign-off
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/01-foundation-and-docs/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm install and verify all gates pass</name>
  <files></files>
  <read_first>
    - /Users/danielvm/Sites/agentic-cps-method/package.json (verify it exists with correct content before running npm install)
    - /Users/danielvm/Sites/agentic-cps-method/bin/install.js (verify it exists before running node --check)
  </read_first>
  <action>
Run the following commands in order from the repo root (`/Users/danielvm/Sites/agentic-cps-method/`). Stop immediately if any command fails and report the error.

1. `npm install` — installs dependencies and creates package-lock.json
2. `npm run lint` — runs markdownlint-cli2 across docs/**/*.md and *.md (excluding CHANGELOG.md)
3. `node --check bin/install.js` — syntax-checks the stub
4. `node bin/install.js` — verify it prints the stub message
5. `grep -r "pnpm\|corepack" docs/` — verify returns no matches

If `npm run lint` fails, identify which file caused the failure and fix the markdownlint violation. Common causes:
- MD013 (line length) — should be disabled by .markdownlint.json; verify that file exists
- MD033 (inline HTML) — should be disabled; verify .markdownlint.json
- MD041 (first heading not H1) — verify file starts with `#` heading

After npm install, commit `package-lock.json` to the repo — it must be tracked for `npm ci` to work in CI.
  </action>
  <verify>
    <automated>cd /Users/danielvm/Sites/agentic-cps-method && npm run lint && node --check bin/install.js && ! grep -r "pnpm\|corepack" docs/ && test -f package-lock.json && echo ALL_GATES_PASS || echo GATES_FAILED</automated>
  </verify>
  <acceptance_criteria>
    - `npm install` exits 0 and creates `package-lock.json`
    - `package-lock.json` is tracked by git (not in .gitignore)
    - `npm run lint` exits 0 (no markdownlint violations)
    - `node --check bin/install.js` exits 0
    - `node bin/install.js` prints "Agentic CPS installer — coming in v0.4.0."
    - `grep -r "pnpm\|corepack" docs/` returns no matches
  </acceptance_criteria>
  <done>npm install succeeds; all lint and syntax checks pass; no pnpm references in docs/</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 deliverables:
    - package.json with correct identity and dependencies
    - bin/install.js stub (node --check verified)
    - .releaserc.json with npmPublish: false
    - .github/workflows/ci.yml (PR lint gate)
    - .github/workflows/release.yml (semantic-release on main)
    - 24 docs files across docs/ tree (all pnpm/corepack refs updated)
    - README.md (fresh, kickass-readme standard)
    - All lint gates passing
  </what-built>
  <how-to-verify>
    1. Review docs/getting-started.md — confirm Requirements section shows only "Node.js ≥ 24" with no pnpm
    2. Review docs/practices/05-develop-architecture-package.md — confirm ADR-002 describes npm (not pnpm)
    3. Review README.md — confirm it describes the project clearly and links to docs/
    4. Check the docs/ tree: `ls docs/phases/ docs/practices/ docs/workflow/ docs/standards/` — verify all expected files are present
    5. Skim .github/workflows/ci.yml — confirm it uses npm (not pnpm) and has the 3 run steps
    6. Review .releaserc.json — confirm "npmPublish": false is present

    Expected file counts:
    - docs/phases/: 3 files (setup.md, production-flow.md, value-activation.md)
    - docs/practices/: 9 files (01 through 09)
    - docs/workflow/: 5 files (discuss.md, spec.md, plan.md, execute.md, homologate.md)
    - docs/standards/: 5 files (conventional-commits.md, code-comments.md, readme.md, clean-code.md, agent-behavior.md)
    - docs/ root: 2 files (getting-started.md, gates.md)
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → node_modules | Third-party packages installed; lockfile pins versions to prevent supply chain attacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | package-lock.json | mitigate | Commit package-lock.json to git. Never .gitignore it. This ensures `npm ci` installs the exact pinned versions in CI, preventing drift. |
| T-06-02 | Tampering | npm install supply chain | accept | devDependencies only (no production deps in Phase 1 beyond @inquirer/prompts stub). Risk is low for dev tooling. SHA-pinning of npm packages is not standard practice. |
</threat_model>

<verification>
Phase 1 is complete when:

1. `npm run lint` exits 0 (all docs/ and *.md pass markdownlint)
2. `node --check bin/install.js` exits 0
3. `grep -r "pnpm\|corepack" docs/` returns no matches
4. All 24 docs files exist in correct paths
5. package-lock.json is committed to the repo
6. Human has approved the docs review checkpoint
</verification>

<success_criteria>
All Phase 1 success criteria from ROADMAP.md are met:
1. `npm install` succeeds and `node bin/install.js --help` (or `node bin/install.js`) executes without error
2. .github/workflows/ has ci.yml (markdownlint + node --check on PRs) and release.yml (semantic-release on main)
3. .releaserc.json configures semantic-release with all required plugins
4. docs/ contains complete methodology coverage: 2 root docs + 3 phase docs + 9 practice docs + 5 workflow docs + 5 standards docs
5. README.md guides first-time visitors to install and get started
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-docs/01-06-SUMMARY.md`
</output>
