---
phase: 01-foundation-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bin/install.js
  - .releaserc.json
  - .gitignore
  - .markdownlint.json
  - .markdownlintignore
autonomous: true
requirements:
  - FOUND-01
  - FOUND-04

must_haves:
  truths:
    - "Running `npm install` in the repo succeeds"
    - "`node --check bin/install.js` exits 0"
    - "`npm run lint` resolves the markdownlint-cli2 binary"
    - "semantic-release reads a valid .releaserc.json configuration"
    - "node_modules/ is excluded by .gitignore"
  artifacts:
    - path: "package.json"
      provides: "npm package identity, bin entry, devDependencies"
      contains: '"name": "acps"'
    - path: "bin/install.js"
      provides: "npx entrypoint stub"
      contains: "coming in v0.4.0"
    - path: ".releaserc.json"
      provides: "semantic-release config with npmPublish: false"
      contains: '"npmPublish": false'
    - path: ".gitignore"
      provides: "node_modules exclusion"
      contains: "node_modules/"
    - path: ".markdownlint.json"
      provides: "markdownlint rule overrides (MD013, MD033, MD041 off)"
      contains: '"MD013": false'
    - path: ".markdownlintignore"
      provides: "exclude CHANGELOG.md and node_modules from lint"
      contains: "CHANGELOG.md"
  key_links:
    - from: "package.json"
      to: "bin/install.js"
      via: 'bin.acps field'
      pattern: '"acps".*bin/install.js'
    - from: "package.json"
      to: "markdownlint-cli2"
      via: "scripts.lint"
      pattern: "markdownlint-cli2"
    - from: ".releaserc.json"
      to: "@semantic-release/npm"
      via: "plugins array"
      pattern: '"npmPublish": false'
---

<objective>
Create the npm package scaffold: package.json with correct identity and dependencies, bin/install.js stub, .releaserc.json for semantic-release, and lint/gitignore configuration files.

Purpose: These files are the foundation that all CI/CD validation depends on. Without package.json, `npm ci` cannot run. Without bin/install.js, the `node --check` gate fails. Without .releaserc.json, releases cannot be automated.

Output: 6 files — package.json, bin/install.js, .releaserc.json, .gitignore (updated), .markdownlint.json, .markdownlintignore
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/01-foundation-and-docs/01-CONTEXT.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/01-foundation-and-docs/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json</name>
  <files>package.json</files>
  <read_first>
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/01-foundation-and-docs/01-RESEARCH.md (Pattern 3: package.json — contains the exact schema to use)
    - /Users/danielvm/Sites/old-agentic-cps-method/package.json (to confirm what NOT to copy — contains "packageManager": "pnpm@10.10.0" which must be excluded)
  </read_first>
  <action>
Create `package.json` at the repo root with exactly this content:

```json
{
  "name": "acps",
  "version": "0.0.0",
  "description": "Agentic CPS — installable CPS methodology skills for AI-powered development teams",
  "type": "module",
  "bin": {
    "acps": "./bin/install.js"
  },
  "scripts": {
    "lint": "markdownlint-cli2 \"docs/**/*.md\" \"*.md\" \"!CHANGELOG.md\""
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.4.1"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.6",
    "@semantic-release/npm": "^13.1.5",
    "markdownlint-cli2": "^0.22.0",
    "semantic-release": "^25.0.3"
  },
  "keywords": ["agentic", "cps", "methodology", "claude-code", "skills", "sdd"],
  "license": "MIT"
}
```

Critical points:
- Do NOT include `"packageManager": "pnpm@10.10.0"` — the old repo has this but D-01 locks npm.
- The lint script uses `markdownlint-cli2` (NOT `markdownlint-cli`) per D-05.
- The lint scope is `"docs/**/*.md" "*.md" "!CHANGELOG.md"` exactly — do not add templates/ or skills/ which don't exist yet.
- `@semantic-release/npm` IS included in devDependencies because .releaserc.json references it (even with npmPublish: false).
- `@semantic-release/github` IS included — the old architecture.md omits it but the .releaserc.json requires it.
- No `"private": true` field — this is a publishable package (even though v1 skips npm publish).
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('/Users/danielvm/Sites/agentic-cps-method/package.json','utf8')); console.log(p.name===('acps') && p.bin.acps==='./bin/install.js' && p.devDependencies['markdownlint-cli2'] && p.devDependencies['@semantic-release/npm'] && !p.packageManager ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at repo root
    - Contains `"name": "acps"`
    - Contains `"bin": { "acps": "./bin/install.js" }`
    - Contains `"markdownlint-cli2"` in devDependencies (NOT `markdownlint-cli`)
    - Contains `"@semantic-release/npm"` in devDependencies
    - Contains `"@semantic-release/github"` in devDependencies
    - Does NOT contain `"packageManager"` key
    - Lint script is exactly: `markdownlint-cli2 "docs/**/*.md" "*.md" "!CHANGELOG.md"`
  </acceptance_criteria>
  <done>package.json exists with correct identity, bin entry, lint script, and all devDependencies including @semantic-release/npm and @semantic-release/github</done>
</task>

<task type="auto">
  <name>Task 2: Create bin/install.js stub and .releaserc.json</name>
  <files>bin/install.js, .releaserc.json</files>
  <read_first>
    - /Users/danielvm/Sites/old-agentic-cps-method/bin/install.js (copy verbatim per D-06)
    - /Users/danielvm/Sites/old-agentic-cps-method/.releaserc.json (copy per D-10, verified content in RESEARCH.md Pattern 4)
  </read_first>
  <action>
**bin/install.js** — Create the `bin/` directory and write `bin/install.js` with exactly this content (copy verbatim from old repo per D-06):

```javascript
#!/usr/bin/env node

// Installer stub — full interactive install implemented in EP-M07 (M-Iter-4).
// This file exists to satisfy CI's `node --check bin/install.js` gate.

console.log('Agentic CPS installer — coming in v0.4.0.');
console.log('For docs: https://github.com/danielvm-ciandt/agentic-cps-method');
```

**.releaserc.json** — Create at repo root (copy from old repo per D-10) with exactly this content:

```json
{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    ["@semantic-release/changelog", {
      "changelogFile": "CHANGELOG.md"
    }],
    ["@semantic-release/npm", { "npmPublish": false }],
    ["@semantic-release/git", {
      "assets": ["package.json", "CHANGELOG.md"],
      "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }],
    ["@semantic-release/github", { "failComment": false, "failTitle": false }]
  ]
}
```

Key points:
- `npmPublish: false` enforces GitHub Releases only — no npm registry publish in v1 (per D-10 and the init decision in STATE.md).
- The `[skip ci]` in the release commit message prevents the release workflow from triggering itself.
- `failComment: false` and `failTitle: false` prevents semantic-release from commenting on closed GitHub issues.
  </action>
  <verify>
    <automated>node --check /Users/danielvm/Sites/agentic-cps-method/bin/install.js && node -e "const r=JSON.parse(require('fs').readFileSync('/Users/danielvm/Sites/agentic-cps-method/.releaserc.json','utf8')); const npmPlugin=r.plugins.find(p=>Array.isArray(p)&&p[0]==='@semantic-release/npm'); console.log(npmPlugin && npmPlugin[1].npmPublish===false ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `bin/install.js` exists and contains `#!/usr/bin/env node`
    - `bin/install.js` contains `coming in v0.4.0`
    - `node --check bin/install.js` exits 0 (no syntax errors)
    - `.releaserc.json` exists at repo root
    - `.releaserc.json` contains `"npmPublish": false`
    - `.releaserc.json` contains `"branches": ["main"]`
    - `.releaserc.json` contains `[skip ci]` in the git message template
  </acceptance_criteria>
  <done>bin/install.js passes node --check; .releaserc.json configures semantic-release with all required plugins and npmPublish: false</done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore and create markdownlint config files</name>
  <files>.gitignore, .markdownlint.json, .markdownlintignore</files>
  <read_first>
    - /Users/danielvm/Sites/agentic-cps-method/.gitignore (current state — only has .DS_Store; must add node_modules/)
    - /Users/danielvm/Sites/old-agentic-cps-method/.markdownlint.json (copy verbatim)
    - /Users/danielvm/Sites/old-agentic-cps-method/.markdownlintignore (copy verbatim)
  </read_first>
  <action>
**.gitignore** — Read the existing `.gitignore` (currently just `.DS_Store`) and extend it by appending:

```
node_modules/
```

Do NOT add `package-lock.json` to .gitignore — the lockfile MUST be committed so `npm ci` works deterministically in CI. The final .gitignore should contain both `.DS_Store` and `node_modules/`.

**.markdownlint.json** — Create at repo root with exactly this content (copied from old repo):

```json
{
  "default": true,
  "MD013": false,
  "MD033": false,
  "MD041": false
}
```

These rules disable: MD013 (line length — docs have long lines), MD033 (inline HTML — some docs use HTML tables), MD041 (first heading — some docs start with frontmatter).

**.markdownlintignore** — Create at repo root with exactly this content (copied from old repo):

```
CHANGELOG.md
node_modules/
```

This prevents markdownlint from trying to lint the auto-generated CHANGELOG.md (which is managed by semantic-release and may violate rules).
  </action>
  <verify>
    <automated>grep -q "node_modules/" /Users/danielvm/Sites/agentic-cps-method/.gitignore && grep -q '\.DS_Store' /Users/danielvm/Sites/agentic-cps-method/.gitignore && grep -q '"MD013": false' /Users/danielvm/Sites/agentic-cps-method/.markdownlint.json && grep -q 'CHANGELOG.md' /Users/danielvm/Sites/agentic-cps-method/.markdownlintignore && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `node_modules/`
    - `.gitignore` still contains `.DS_Store` (not replaced, extended)
    - `.gitignore` does NOT contain `package-lock.json`
    - `.markdownlint.json` exists and contains `"MD013": false`, `"MD033": false`, `"MD041": false`
    - `.markdownlintignore` exists and contains `CHANGELOG.md` and `node_modules/`
  </acceptance_criteria>
  <done>.gitignore excludes node_modules but retains .DS_Store; markdownlint configured to match old repo behavior that passes existing docs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → npm registry | Package metadata is published; malicious content here could mislead users |
| .releaserc.json → GitHub Actions | Release config is executed in CI with write permissions; misconfiguration can corrupt releases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | .releaserc.json plugins array | mitigate | Use exact plugin versions in devDependencies; pin to specific semver ranges (^major.minor.patch) rather than latest |
| T-01-02 | Information Disclosure | bin/install.js (stub) | accept | Stub prints repo URL — public info, no secrets, no risk |
| T-01-03 | Elevation of Privilege | package.json bin entry | accept | bin entry points to a stub with no privileged operations; full installer is Phase 4 |
</threat_model>

<verification>
After all three tasks complete:

1. `node --check bin/install.js` exits 0
2. `node bin/install.js` prints "Agentic CPS installer — coming in v0.4.0."
3. `cat package.json | grep '"name"'` returns `"name": "acps"`
4. `cat .releaserc.json | grep npmPublish` returns `"npmPublish": false`
5. `cat .gitignore` contains both `.DS_Store` and `node_modules/`
6. `cat .markdownlint.json` shows MD013/MD033/MD041 as false
</verification>

<success_criteria>
- All 6 files exist: package.json, bin/install.js, .releaserc.json, .gitignore (updated), .markdownlint.json, .markdownlintignore
- `node --check bin/install.js` exits 0
- package.json has correct name, bin, scripts, engines, dependencies, devDependencies
- .releaserc.json has npmPublish: false and all 6 plugins
- .gitignore excludes node_modules/ without removing .DS_Store
- .markdownlintignore excludes CHANGELOG.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-docs/01-01-SUMMARY.md`
</output>
