---
phase: 05-scope-intelligence
plan: 04
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified: []
autonomous: false
requirements:
  - SCOPE-01
  - SCOPE-02
  - INTEL-01
  - INTEL-02
  - INTEL-03

must_haves:
  truths:
    - "npm run lint exits 0 with 0 errors across all 10 new skill Markdown files"
    - "All 5 skill directories exist with exactly SKILL.md and workflow.md each"
    - "Human has verified structural correctness of all 5 skills by spot-checking thin SKILL.md format and workflow.md XML structure"
    - "Phase 5 is declared complete"
  artifacts:
    - path: "skills/acps-change-request/SKILL.md"
      provides: "Scope skill — CR registration"
      contains: "name: acps-change-request"
    - path: "skills/acps-scope-manager/SKILL.md"
      provides: "Scope skill — ledger display"
      contains: "name: acps-scope-manager"
    - path: "skills/acps-document-project/SKILL.md"
      provides: "Intel skill — deep scan"
      contains: "name: acps-document-project"
    - path: "skills/acps-scan/SKILL.md"
      provides: "Intel skill — quick scan"
      contains: "name: acps-scan"
    - path: "skills/acps-code-map/SKILL.md"
      provides: "Intel skill — parallel code map"
      contains: "name: acps-code-map"
  key_links:
    - from: "npm run lint"
      to: "skills/**/*.md"
      via: "markdownlint-cli2 glob covering all 10 new files"
      pattern: "skills/\\*\\*/\\*.md"
---

<objective>
Run the lint gate and structural verification across all 5 new Phase 5 skills. Human approves that the 10 skill files are correctly formed before phase completion is declared.

Purpose: Enforce D-14 — all new workflow.md files pass `npm run lint` at 0 errors. Ensure the phase's 5 skills are structurally correct and ready for installer deployment.
Output: Verified lint pass; human checkpoint approval; phase declared complete.
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scope-intelligence/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run lint gate on all 10 new Phase 5 skill files</name>
  <files></files>
  <read_first>
    - .planning/phases/05-scope-intelligence/05-CONTEXT.md (D-14: lint gate requirement)
    - package.json (confirm lint script glob covers skills/**/*.md)
  </read_first>
  <action>
Run the lint gate command from the project root:

```
npm run lint
```

The `lint` script is `markdownlint-cli2 "docs/**/*.md" "templates/**/*.md" "skills/**/*.md" "*.md" "!CHANGELOG.md"` which automatically covers all 10 new files:
- `skills/acps-change-request/SKILL.md`
- `skills/acps-change-request/workflow.md`
- `skills/acps-scope-manager/SKILL.md`
- `skills/acps-scope-manager/workflow.md`
- `skills/acps-document-project/SKILL.md`
- `skills/acps-document-project/workflow.md`
- `skills/acps-scan/SKILL.md`
- `skills/acps-scan/workflow.md`
- `skills/acps-code-map/SKILL.md`
- `skills/acps-code-map/workflow.md`

If lint reports errors: fix each reported violation in the flagged file before proceeding.
Common violations to watch for:
- MD041: First heading must be level 1 (workflow.md starts with `# ` heading — ensure it does)
- MD013: Line length (if line length rule is enabled — check .markdownlint.json or .markdownlint-cli2.jsonc for override)
- MD031: Fenced code block surroundeded by blank lines
- MD032: Lists surrounded by blank lines

After fixing all violations, re-run `npm run lint` and confirm exit code 0.
  </action>
  <verify>
    <automated>cd /Users/danielvm/Sites/agentic-cps-method && npm run lint 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits with code 0
    - Output shows 0 errors across all skill files
    - All 10 new Phase 5 skill files exist (verify with `ls skills/acps-change-request/ skills/acps-scope-manager/ skills/acps-document-project/ skills/acps-scan/ skills/acps-code-map/`)
  </acceptance_criteria>
  <done>npm run lint exits 0 with no errors on any of the 10 new Phase 5 skill Markdown files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human structural verification of all 5 Phase 5 skills</name>
  <what-built>
5 new skills were created in Wave 1 (plans 05-01, 05-02, 05-03):
- `skills/acps-change-request/` — registers CRs with BCP/FP delta in change-log.md, warns on overrun
- `skills/acps-scope-manager/` — displays full scope ledger with overrun banner
- `skills/acps-document-project/` — deep scan → 4 docs in .planning/intel/ with resume support
- `skills/acps-scan/` — 1-page quick context summary for AI agents (independent)
- `skills/acps-code-map/` — parallel per-module analysis → .planning/codebase/

Lint passed at 0 errors in Task 1.
  </what-built>
  <how-to-verify>
Spot-check each skill using these exact checks:

**Structural checks (run in project root):**
```
ls skills/acps-change-request/ skills/acps-scope-manager/ skills/acps-document-project/ skills/acps-scan/ skills/acps-code-map/
```
Each should show exactly: `SKILL.md  workflow.md`

**Thin SKILL.md format check (each should be 6 lines):**
```
cat skills/acps-change-request/SKILL.md
cat skills/acps-scope-manager/SKILL.md
```
Expected: YAML front-matter block with `name:`, `description:`, then `Follow the instructions in ./workflow.md.`

**workflow.md XML structure check:**
```
grep -n "<workflow>\|</workflow>" skills/acps-change-request/workflow.md
grep -n "<workflow>\|</workflow>" skills/acps-scope-manager/workflow.md
grep -n "<workflow>\|</workflow>" skills/acps-document-project/workflow.md
grep -n "<workflow>\|</workflow>" skills/acps-scan/workflow.md
grep -n "<workflow>\|</workflow>" skills/acps-code-map/workflow.md
```
Each should show exactly 2 lines: one `<workflow>` opening and one `</workflow>` closing.

**Key decision verification:**
- `grep "CR-" skills/acps-change-request/workflow.md` → shows CR ID format (D-03)
- `grep "OVERRUN" skills/acps-change-request/workflow.md` → shows overrun warning (D-04)
- `grep "baseline_bcp" skills/acps-scope-manager/workflow.md` → shows ledger header (D-05)
- `grep "scan-state.json" skills/acps-document-project/workflow.md` → shows resume support (D-07)
- `grep "notes.md" skills/acps-scan/workflow.md` → shows impediment reading (D-09)
- `grep "MANIFEST.md" skills/acps-code-map/workflow.md` → shows manifest output (D-12)
- `grep "intel/" skills/acps-scan/workflow.md` → should return EMPTY (D-10: independence)

**Count check:**
```
ls skills/ | wc -l
```
Should show 30 total skill directories (25 from Phase 4 + 5 new Phase 5 skills).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found so they can be fixed before phase completion.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdownlint → skill files | Lint tool reads Markdown files to check syntax; read-only operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | markdownlint config (.markdownlint.json) | accept | Config file is version-controlled; any rule disabling is visible in git history. |
| T-05-04-02 | Denial of Service | Lint taking excessive time on large skill set | accept | 30 skills × 2 files = 60 Markdown files; markdownlint-cli2 handles this in seconds. |
</threat_model>

<verification>
Phase 5 is complete when:

1. `npm run lint` exits 0 — no Markdown violations in any of the 10 new skill files
2. `ls skills/` shows 30 directories (25 prior + 5 Phase 5)
3. Each of the 5 new skill dirs contains exactly `SKILL.md` + `workflow.md`
4. Human checkpoint approved — structural spot-check passed
5. All 5 requirement IDs confirmed covered: SCOPE-01, SCOPE-02, INTEL-01, INTEL-02, INTEL-03
</verification>

<success_criteria>
- `npm run lint` exits 0 with 0 errors on all Phase 5 skill files (D-14)
- 5 new skill directories exist with 2 files each = 10 new files total
- Human approves structural correctness via checkpoint
- Phase 5 declared complete: teams can manage scope changes and run project intelligence scans
</success_criteria>

<output>
After completion, create `.planning/phases/05-scope-intelligence/05-04-SUMMARY.md`
</output>
