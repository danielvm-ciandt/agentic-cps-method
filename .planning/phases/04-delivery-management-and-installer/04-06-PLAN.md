---
phase: 04-delivery-management-and-installer
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/acps-new-iteration/SKILL.md
  - skills/acps-new-iteration/workflow.md
  - skills/acps-note/SKILL.md
  - skills/acps-note/workflow.md
  - skills/acps-help/SKILL.md
  - skills/acps-help/workflow.md
autonomous: true
requirements:
  - DELIV-09
  - DELIV-13
  - DELIV-14

must_haves:
  truths:
    - "skills/acps-new-iteration/ exists with SKILL.md and workflow.md"
    - "acps-new-iteration opens a new iteration: creates an iteration branch, assigns pending epics to the iteration"
    - "skills/acps-note/ exists with SKILL.md and workflow.md"
    - "acps-note appends a timestamped entry to {planningDir}/notes.md with zero friction (no file selection, no prompts beyond the note text)"
    - "skills/acps-help/ exists with SKILL.md and workflow.md"
    - "acps-help outputs all acps skills grouped by phase (Setup, Loop, Delivery, Management) with 1-line description each and a next recommended skill based on .acps-config.json"
  artifacts:
    - path: "skills/acps-new-iteration/SKILL.md"
      provides: "Skill entry point for iteration management"
      contains: "name: acps-new-iteration"
    - path: "skills/acps-new-iteration/workflow.md"
      provides: "New iteration workflow with branch creation and epic assignment"
      contains: "<workflow>"
    - path: "skills/acps-note/SKILL.md"
      provides: "Skill entry point for zero-friction note capture"
      contains: "name: acps-note"
    - path: "skills/acps-note/workflow.md"
      provides: "Note capture workflow appending to notes.md"
      contains: "<workflow>"
    - path: "skills/acps-help/SKILL.md"
      provides: "Skill entry point for methodology overview"
      contains: "name: acps-help"
    - path: "skills/acps-help/workflow.md"
      provides: "Help workflow listing all skills grouped by phase with next recommended skill"
      contains: "<workflow>"
  key_links:
    - from: "skills/acps-note/workflow.md"
      to: "{planningDir}/notes.md"
      via: "append write action"
      pattern: "notes.md"
    - from: "skills/acps-help/workflow.md"
      to: ".acps-config.json"
      via: "read for next recommended skill"
      pattern: ".acps-config.json"
---

<objective>
Create the `acps-new-iteration`, `acps-note`, and `acps-help` skills — the utility trio. These are lighter-weight skills focused on iteration management, zero-friction capture, and onboarding orientation.

Purpose: Teams need to open new iterations without ceremony, capture fleeting ideas without friction, and orient themselves in the methodology at any time. These three skills cover these everyday needs.

Output: 6 files — SKILL.md + workflow.md for each of the three skills.
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/04-delivery-management-and-installer/04-CONTEXT.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/04-delivery-management-and-installer/04-RESEARCH.md

<interfaces>
<!-- acps-branch workflow — git checkout -b pattern to replicate for iteration branch -->
<!-- skills/acps-branch/workflow.md — branch creation with confirmation -->

<!-- D-19: acps-help groups skills by phase -->
<!-- Setup Phase: acps-init, acps-vision, acps-backlog, acps-architecture, acps-project-roadmap -->
<!-- Loop Phase: acps-branch, acps-discuss, acps-spec, acps-plan, acps-execute, acps-homologate -->
<!-- Delivery: acps-deliverable, acps-gate, acps-status, acps-report -->
<!-- Management: acps-new-epic, acps-new-story, acps-new-bug, acps-bug-fix, acps-new-iteration, acps-pr, acps-pause, acps-resume, acps-note, acps-help -->

<!-- notes.md append format -->
<!-- ## {ISO timestamp} — {category} -->
<!-- {note text} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skills/acps-new-iteration/ and skills/acps-note/ (SKILL.md + workflow.md each)</name>
  <files>skills/acps-new-iteration/SKILL.md, skills/acps-new-iteration/workflow.md, skills/acps-note/SKILL.md, skills/acps-note/workflow.md</files>

  <read_first>
    - skills/acps-init/SKILL.md — exact SKILL.md format to replicate
    - skills/acps-branch/workflow.md — git checkout -b pattern with confirmation; replicate for iteration branch creation
    - .planning/phases/04-delivery-management-and-installer/04-CONTEXT.md — DELIV-09 (open new iteration branch, assign pending epics), DELIV-13 (zero-friction capture, timestamped, notes.md)
  </read_first>

  <action>
**acps-new-iteration:**

Create `skills/acps-new-iteration/SKILL.md`:

```markdown
---
name: acps-new-iteration
description: 'Opens a new iteration: creates the iteration branch and assigns pending epics to the new iteration. Run at the start of each new delivery iteration.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-new-iteration/workflow.md` with 5 XML steps:

**Step 1: Load config and read backlog** — read `.acps-config.json` for `planningDir`; fall back to `.planning`. Read `{planningDir}/backlog.md` to get current iteration number and list of pending epics. If backlog.md does not exist: output "Run `acps-backlog` first to define your epics and iterations." HALT.

**Step 2: Collect iteration details** — output current state: "Current highest iteration: iter-{N}". Ask:
- "New iteration number? (default: {N+1})"
- "Iteration goal? (1-2 sentence description of what this iteration delivers)"
- "Which pending epics to assign to this iteration? (list epic IDs from backlog, comma-separated)"

**Step 3: Create iteration branch** — compose `branch_name = iter-{N}` (the iteration base branch). Display: "Branch to create: `{branch_name}`". Ask: "Confirm? (Enter to confirm, or type a correction)". Run `git checkout -b {branch_name}`. If git fails: output the error and HALT.

**Step 4: Update backlog.md** — append a new iteration section to `{planningDir}/backlog.md`:

```markdown
## Iteration {N} — {iteration_goal}

**Status:** active
**Epics:** {epic_id_1}, {epic_id_2}

| Epic ID | Title | BCP | Status |
|---------|-------|-----|--------|
| {epic_id} | {epic title from backlog} | {bcp} | pending |
```

**Step 5: Report completion** — output:

```
**acps-new-iteration complete.**

Iteration: iter-{N}
Branch: {branch_name}
Goal: {iteration_goal}
Assigned epics: {epic_ids}

Backlog updated: {planningDir}/backlog.md

Next step: Run `acps-branch` for each story you start within this iteration.
```

---

**acps-note:**

Create `skills/acps-note/SKILL.md`:

```markdown
---
name: acps-note
description: 'Zero-friction idea and impediment capture. Appends a timestamped note to {planningDir}/notes.md immediately. No file selection, no preamble.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-note/workflow.md` with 4 XML steps:

**Step 1: Load config** — read `.acps-config.json` for `planningDir`; fall back to `.planning`.

**Step 2: Capture the note** — ask immediately, with no preamble:
- "Note: " (single-line prompt for the note text)
- "Category? (idea / impediment / decision / question — default: idea)"

**Step 3: Append to notes.md** — check if `{planningDir}/notes.md` exists. If not: create it with a header:

```markdown
# Project Notes

Captured ideas, impediments, decisions, and questions.

---
```

Append the note entry:

```markdown
## {ISO 8601 timestamp} — {category}

{note text}

---
```

**Step 4: Confirm capture** — output:

```
Captured. ({planningDir}/notes.md)
```

One line. No further output. Minimum friction.
  </action>

  <verify>
    <automated>grep -c "name: acps-new-iteration" /Users/danielvm/Sites/agentic-cps-method/skills/acps-new-iteration/SKILL.md && grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-new-iteration/workflow.md && grep -c "git checkout -b" /Users/danielvm/Sites/agentic-cps-method/skills/acps-new-iteration/workflow.md && grep -c "name: acps-note" /Users/danielvm/Sites/agentic-cps-method/skills/acps-note/SKILL.md && grep -c "notes.md" /Users/danielvm/Sites/agentic-cps-method/skills/acps-note/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - `skills/acps-new-iteration/SKILL.md` exists and contains exactly `name: acps-new-iteration`
    - `skills/acps-new-iteration/workflow.md` exists and contains `<workflow>` and `</workflow>`
    - `skills/acps-new-iteration/workflow.md` contains `git checkout -b` (branch creation)
    - `skills/acps-new-iteration/workflow.md` contains `backlog.md` (read and update)
    - `skills/acps-new-iteration/workflow.md` contains `HALT` (for missing backlog.md case)
    - `skills/acps-note/SKILL.md` exists and contains exactly `name: acps-note`
    - `skills/acps-note/workflow.md` exists and contains `<workflow>` and `</workflow>`
    - `skills/acps-note/workflow.md` contains at least 4 `<step n=` entries
    - `skills/acps-note/workflow.md` contains `notes.md` (the target file)
    - `skills/acps-note/workflow.md` contains category options: `idea`, `impediment`, `decision`, `question`
    - All 4 SKILL.md files contain `Follow the instructions in ./workflow.md.`
  </acceptance_criteria>

  <done>acps-new-iteration and acps-note skills created: new-iteration creates iteration branch and updates backlog.md; acps-note is minimal 4-step skill appending timestamped notes to notes.md with single-line confirmation per DELIV-09 and DELIV-13.</done>
</task>

<task type="auto">
  <name>Task 2: Create skills/acps-help/ (SKILL.md + workflow.md)</name>
  <files>skills/acps-help/SKILL.md, skills/acps-help/workflow.md</files>

  <read_first>
    - skills/acps-init/SKILL.md — exact SKILL.md format to replicate
    - skills/acps-execute/workflow.md — step structure with action, check if, output elements
    - .planning/phases/04-delivery-management-and-installer/04-CONTEXT.md — D-19 (all skills grouped by phase: Setup, Loop, Delivery, Management; 1-line description each; next recommended skill based on .acps-config.json state)
    - .planning/REQUIREMENTS.md — all skill descriptions in DELIV-01 through DELIV-14, SETUP-01 through SETUP-05, LOOP-01 through LOOP-06 — read the exact descriptions to use in the help output
  </read_first>

  <action>
Create `skills/acps-help/SKILL.md`:

```markdown
---
name: acps-help
description: 'Displays the full acps skill map grouped by phase with 1-line descriptions, then recommends the next skill based on your current project state.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-help/workflow.md` with 4 XML steps:

**Step 1: Load project state** — read `.acps-config.json` in the project root. If missing: set project_state to "not initialized". If exists: extract `planningDir`. Check for `{planningDir}/backlog.md` (exists = setup phase complete). Check for `{planningDir}/continue-here.md` (exists = paused session). Run `git branch --show-current` to detect active iteration/story branch.

**Step 2: Display full skill map** — output the complete skill map grouped by phase (D-19):

```
**acps — Skill Map**

## Setup Phase (run once per project)

- `acps-init`             — Initialize project: create .planning/ structure and .acps-config.json
- `acps-vision`           — Write project vision following CPS Ch.4 discipline
- `acps-backlog`          — Define epics and iterations, write backlog.md
- `acps-architecture`     — Capture architecture package following CPS Ch.14
- `acps-project-roadmap`  — BCP/FP estimation on all epics, write roadmap.md

## Loop Phase (repeat per story)

- `acps-branch`           — Create semantic branch: iter-{N}/{epic-id}-{slug}
- `acps-discuss`          — Extract CPS implementation decisions for the story
- `acps-spec`             — Write acceptance criteria; enforce ambiguity gate ≤ 0.20
- `acps-plan`             — Task breakdown with BCP estimates
- `acps-execute`          — Implement story tasks; record session telemetry
- `acps-homologate`       — Interactive UAT: walk ACs, capture pass/fail, write totals

## Delivery

- `acps-deliverable`      — Generate client deliverable from template; human reviews before sending
- `acps-gate`             — List gate blockers; approve to lock BCP baseline in change-log.md
- `acps-status`           — Show current phase, iteration, epic, and scope balance
- `acps-report`           — Generate daily/weekly/monthly/quarterly report from template

## Management

- `acps-new-epic`         — Create epic in lean-spec format from templates/specs/epic.md
- `acps-new-story`        — Create story under an epic with stage and BCP set
- `acps-new-bug`          — Register bug before fixing; link to epic; assign BCP credit
- `acps-bug-fix`          — Bug fix workflow: branch (bug/{id}-{slug}) → fix → verify → close
- `acps-new-iteration`    — Open new iteration: branch + assign pending epics
- `acps-pr`               — Create PR with conventional commit title; filter .planning/ commits
- `acps-pause`            — Save work state to continue-here.md for cross-session handoff
- `acps-resume`           — Restore work state from continue-here.md; staleness check
- `acps-note`             — Zero-friction timestamped capture → notes.md
- `acps-help`             — This skill: methodology overview + next recommended skill
```

**Step 3: Recommend next skill** — based on project state from Step 1:
- No .acps-config.json → recommend: `acps-init`
- .acps-config.json exists, no backlog.md → recommend: `acps-vision` then `acps-backlog`
- backlog.md exists, continue-here.md exists → recommend: `acps-resume`
- On an iter-{N}/{epic-id}-{slug} branch → recommend: next loop skill based on git log (no commits yet → `acps-discuss`; has commits → check story status)
- On main/default branch with backlog → recommend: `acps-branch` to start next story

**Step 4: Output recommendation** — display concise recommendation:

```
**Recommended next step:**

{skill name} — {reason based on detected state}

Run it by typing its name in your AI IDE (e.g., "acps-discuss" or "@acps-discuss").
```
  </action>

  <verify>
    <automated>grep -c "name: acps-help" /Users/danielvm/Sites/agentic-cps-method/skills/acps-help/SKILL.md && grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-help/workflow.md && grep -c "Setup Phase\|Loop Phase\|Delivery\|Management" /Users/danielvm/Sites/agentic-cps-method/skills/acps-help/workflow.md && grep -c "acps-init\|acps-branch\|acps-gate\|acps-pause" /Users/danielvm/Sites/agentic-cps-method/skills/acps-help/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - `skills/acps-help/SKILL.md` exists and contains exactly `name: acps-help`
    - `skills/acps-help/SKILL.md` contains `Follow the instructions in ./workflow.md.`
    - `skills/acps-help/workflow.md` exists and contains `<workflow>` and `</workflow>`
    - `skills/acps-help/workflow.md` contains at least 4 `<step n=` entries
    - `skills/acps-help/workflow.md` contains `Setup Phase` (first skill group)
    - `skills/acps-help/workflow.md` contains `Loop Phase` (second skill group)
    - `skills/acps-help/workflow.md` contains `acps-init` (listed in Setup group)
    - `skills/acps-help/workflow.md` contains `acps-gate` (listed in Delivery group)
    - `skills/acps-help/workflow.md` contains `acps-pause` (listed in Management group)
    - `skills/acps-help/workflow.md` contains `continue-here.md` (state detection for recommendation)
    - `skills/acps-help/workflow.md` contains all 25 skill names (5 Setup + 6 Loop + 4 Delivery + 10 Management)
  </acceptance_criteria>

  <done>acps-help skill created: SKILL.md has thin frontmatter + delegate line; workflow.md has 4 steps that read project state, output all 25 skills grouped by phase per D-19, and recommend the next skill based on detected project state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill→filesystem | acps-new-iteration creates branch and appends to backlog.md; acps-note appends to notes.md; acps-help reads .acps-config.json and backlog.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | backlog.md modification by acps-new-iteration | accept | Append-only iteration block; user reviews; no external input injected |
| T-04-06-02 | Tampering | notes.md append by acps-note | accept | User-generated content; append-only; no code execution; low risk |
| T-04-06-03 | Information Disclosure | project state in acps-help output | accept | Help output is shown to the authenticated IDE user only; no external transmission |
</threat_model>

<verification>
- `ls skills/acps-new-iteration/` returns `SKILL.md workflow.md`
- `grep "name: acps-new-iteration" skills/acps-new-iteration/SKILL.md` exits 0
- `grep "git checkout -b" skills/acps-new-iteration/workflow.md` exits 0
- `ls skills/acps-note/` returns `SKILL.md workflow.md`
- `grep "name: acps-note" skills/acps-note/SKILL.md` exits 0
- `grep "notes.md" skills/acps-note/workflow.md` exits 0
- `ls skills/acps-help/` returns `SKILL.md workflow.md`
- `grep "name: acps-help" skills/acps-help/SKILL.md` exits 0
- `grep "Loop Phase" skills/acps-help/workflow.md` exits 0
- `npm run lint` exits 0
</verification>

<success_criteria>
- All 6 files created: SKILL.md + workflow.md for acps-new-iteration, acps-note, acps-help
- acps-new-iteration: creates iter-{N} branch and updates backlog.md with iteration section per DELIV-09
- acps-note: 4-step minimal workflow, appends timestamped note to notes.md, single-line confirmation per DELIV-13
- acps-help: lists all 25 skills (5+6+4+10) grouped by phase per D-19, recommends next skill from project state per DELIV-14
- All SKILL.md files: thin frontmatter (name + description) + delegate line only
- `npm run lint` 0 errors on new files
</success_criteria>

<output>
After completion, create `.planning/phases/04-delivery-management-and-installer/04-06-SUMMARY.md`
</output>
