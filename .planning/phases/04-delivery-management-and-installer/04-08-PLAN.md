---
phase: 04-delivery-management-and-installer
plan: 08
type: execute
wave: 3
depends_on:
  - "04-07"
files_modified: []
autonomous: false
requirements:
  - INST-01
  - INST-02
  - INST-03
  - INST-04
  - DELIV-01
  - DELIV-02
  - DELIV-03
  - DELIV-04
  - DELIV-05
  - DELIV-06
  - DELIV-07
  - DELIV-08
  - DELIV-09
  - DELIV-10
  - DELIV-11
  - DELIV-12
  - DELIV-13
  - DELIV-14

must_haves:
  truths:
    - "npm run lint exits 0 with no markdownlint errors on all new Phase 4 skill files"
    - "node --check bin/install.js exits 0 (ESM syntax valid)"
    - "node bin/install.js --help exits 0 and prints usage summary"
    - "All 14 new skills exist: each has SKILL.md with correct name field and workflow.md with <workflow> element"
    - "grep -l workflow skills/*/workflow.md counts 25 files (11 from Phase 3 + 14 new)"
    - "node bin/install.js --claude --local --estimation bcp_full --lang en --doc-lang en runs to completion and produces .acps-config.json with all required fields"
    - "Human verifies: .acps-config.json exists with version, name, lang, docLang, estimation, runtimes, location, installed_at"
    - "Human verifies: .claude/skills/ contains the copied skill directories from the local install"
  artifacts:
    - path: "skills/acps-gate/SKILL.md"
      provides: "acps-gate entry point"
      contains: "name: acps-gate"
    - path: "skills/acps-pause/SKILL.md"
      provides: "acps-pause entry point"
      contains: "name: acps-pause"
    - path: "skills/acps-resume/SKILL.md"
      provides: "acps-resume entry point"
      contains: "name: acps-resume"
    - path: "bin/install.js"
      provides: "Full installer"
      contains: "IDE_PATHS"
  key_links:
    - from: "bin/install.js"
      to: ".acps-config.json"
      via: "non-interactive install test"
      pattern: "installed_at"
    - from: "npm run lint"
      to: "skills/**/*.md"
      via: "markdownlint-cli2 glob"
      pattern: "skills/**/*.md"
---

<objective>
Run the Phase 4 lint gate, structural verification of all 14 new skills, smoke-test the installer in non-interactive mode, and present the human checkpoint for final approval.

Purpose: All prior plans in this phase create files; this plan verifies they are correct before the phase is marked complete. The human checkpoint ensures the installed skills actually land in the right place.

Output: Verified phase state + human approval.
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/04-delivery-management-and-installer/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run lint gate and structural verification</name>
  <files></files>

  <read_first>
    - package.json — confirm lint script glob covers skills/**/*.md
    - .markdownlint.json — confirm MD033 is false (XML allowed) and other active rules
  </read_first>

  <action>
Run the following verification commands in sequence. Report each result. Fix any failure before proceeding to the next step.

**1. Lint gate:**

```bash
npm run lint
```

Expected: exit 0, zero errors. If errors appear, read the error messages, fix the offending lines in the skill files (most common issues: trailing spaces MD009, missing blank lines before headings MD022, bare URLs MD034), and re-run until clean.

**2. Node syntax check:**

```bash
node --check bin/install.js
```

Expected: exit 0. If it fails, read the error and fix the syntax in bin/install.js.

**3. Skills count:**

```bash
ls -d skills/acps-*/  | wc -l
```

Expected: 25 (11 from Phase 3 + 14 new from Phase 4). List the 14 new skill directories:
acps-gate, acps-status, acps-pause, acps-resume, acps-new-epic, acps-new-story, acps-new-bug, acps-bug-fix, acps-pr, acps-deliverable, acps-report, acps-new-iteration, acps-note, acps-help.

**4. Structural check — SKILL.md name fields:**

```bash
grep -l "name: acps-" skills/*/SKILL.md | wc -l
```

Expected: 25 (all skills have name field). Also verify the 14 new skills specifically:

```bash
for skill in acps-gate acps-status acps-pause acps-resume acps-new-epic acps-new-story acps-new-bug acps-bug-fix acps-pr acps-deliverable acps-report acps-new-iteration acps-note acps-help; do
  grep -q "name: ${skill}" "skills/${skill}/SKILL.md" && echo "OK: ${skill}" || echo "FAIL: ${skill} missing name"
done
```

**5. Structural check — workflow.md presence:**

```bash
grep -l "<workflow>" skills/*/workflow.md | wc -l
```

Expected: 25. Any file missing `<workflow>` must be fixed before the human checkpoint.

**6. --help smoke test:**

```bash
node bin/install.js --help
```

Expected: prints usage summary and exits 0.

**7. Non-interactive install smoke test (local mode into temp dir):**

Run:
```bash
node bin/install.js --claude --local --estimation bcp_full --lang en --doc-lang en
```

Expected: runs to completion without hanging on a prompt, prints "acps installed successfully", and exits 0.

After running, verify:
```bash
cat .acps-config.json
```

Expected: JSON object containing all 8 fields: `version`, `name`, `lang`, `docLang`, `estimation`, `runtimes`, `location`, `installed_at`. The `planningDir` field should be present if `.acps-config.json` existed before (from acps-init). If the file was just created, `planningDir` may be absent — that is acceptable since acps-init sets it.

Verify skills were copied:
```bash
ls .claude/skills/ | head -5
```

Expected: lists skill directories starting with `acps-`.
  </action>

  <verify>
    <automated>cd /Users/danielvm/Sites/agentic-cps-method && npm run lint && node --check bin/install.js && node bin/install.js --help</automated>
  </verify>

  <acceptance_criteria>
    - `npm run lint` exits 0 with zero errors
    - `node --check bin/install.js` exits 0
    - `node bin/install.js --help` exits 0 and outputs text containing `Usage:`
    - `ls -d skills/acps-*/ | wc -l` outputs 25
    - All 14 new skills have `<workflow>` in their workflow.md
    - All 14 new skills have correct `name: acps-{skill}` in their SKILL.md
    - `node bin/install.js --claude --local --estimation bcp_full --lang en --doc-lang en` exits 0
    - `.acps-config.json` contains `"installed_at"` field after the non-interactive run
    - `.acps-config.json` contains `"runtimes": ["claude-code"]` after the non-interactive run
    - `.claude/skills/` directory exists and contains skill subdirectories
  </acceptance_criteria>

  <done>All automated checks pass: lint 0 errors, node --check valid, 25 skills present with correct structure, non-interactive installer runs to completion and produces correct .acps-config.json and .claude/skills/ directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 4 skills (14 new skills in skills/) and the full bin/install.js installer are complete and verified by automated checks. The non-interactive installer was run with --claude --local flags and should have:

1. Created or updated `.acps-config.json` at the project root
2. Copied all skill directories to `.claude/skills/`
  </what-built>

  <how-to-verify>

**Check 1: .acps-config.json is correct**

Open `.acps-config.json` at the project root. Verify it contains:

```json
{
  "version": "0.0.0",
  "name": "my-project",
  "lang": "en",
  "docLang": "en",
  "estimation": "bcp_full",
  "runtimes": ["claude-code"],
  "location": "local",
  "installed_at": "..."
}
```

All 7 installer fields must be present (planningDir may also be present if acps-init ran previously).

**Check 2: Skills were copied to .claude/skills/**

Run in terminal:

```bash
ls .claude/skills/
```

You should see all 25 skill directories (acps-branch, acps-bug-fix, acps-deliverable, acps-discuss, acps-execute, acps-gate, acps-help, acps-homologate, acps-init, acps-new-bug, acps-new-epic, acps-new-iteration, acps-new-story, acps-note, acps-pause, acps-plan, acps-pr, acps-project-roadmap, acps-report, acps-resume, acps-spec, acps-status, acps-vision, acps-architecture, acps-backlog).

**Check 3: Spot-check a new skill**

Open `.claude/skills/acps-gate/SKILL.md`. It should have `name: acps-gate` and the delegate line.

Open `.claude/skills/acps-pause/workflow.md`. It should contain `continue-here.md`.

**Check 4: Lint is clean**

Run: `npm run lint`

Expected output: no errors (exit 0).
  </how-to-verify>

  <resume-signal>Type "approved" if all 4 checks pass. Describe any failures to fix before approving.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI→filesystem | Smoke test installer writes to .claude/skills/ and .acps-config.json in the project root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Tampering | .claude/skills/ overwritten during smoke test | accept | Test copies from skills/ into .claude/skills/; idempotent; no destructive operations |
| T-04-08-02 | Information Disclosure | .acps-config.json written with test values | accept | Test values (name: my-project) are non-sensitive; file is in user-controlled repo |
</threat_model>

<verification>
- `npm run lint` exits 0
- `node --check bin/install.js` exits 0
- `node bin/install.js --help` exits 0
- `ls -d skills/acps-*/ | wc -l` outputs 25
- `.acps-config.json` contains `installed_at`
- `.claude/skills/` contains skill directories
- Human checkpoint: approved
</verification>

<success_criteria>
- Zero markdownlint errors on all Phase 4 files
- All 14 new skill directories exist with correct SKILL.md name fields and workflow.md with `<workflow>`
- `node bin/install.js --claude --local --estimation bcp_full --lang en --doc-lang en` completes without prompts
- `.acps-config.json` written with all 8 installer fields
- `.claude/skills/` populated with all 25 skill directories
- Human reviews and types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/04-delivery-management-and-installer/04-08-SUMMARY.md`
</output>
