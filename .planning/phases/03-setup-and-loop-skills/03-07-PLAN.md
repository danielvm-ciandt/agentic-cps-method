---
phase: 03-setup-and-loop-skills
plan: 07
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
  - 03-06
files_modified:
  - package.json
autonomous: false
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - SETUP-05
  - LOOP-01
  - LOOP-02
  - LOOP-03
  - LOOP-04
  - LOOP-05
  - LOOP-06

must_haves:
  truths:
    - "All 11 skill directories exist under skills/ with exactly SKILL.md and workflow.md each"
    - "npm run lint passes with skills/**/*.md included"
    - "Every SKILL.md has name: and description: frontmatter plus the workflow delegate line"
    - "Every workflow.md has <workflow> root element and at least 4 <step> blocks"
    - "User has verified at least one complete skill pair (SKILL.md + workflow.md) for correctness"
  artifacts:
    - path: "skills/acps-init/SKILL.md"
      provides: "Init skill"
      contains: "name: acps-init"
    - path: "skills/acps-vision/SKILL.md"
      provides: "Vision skill"
      contains: "name: acps-vision"
    - path: "skills/acps-backlog/SKILL.md"
      provides: "Backlog skill"
      contains: "name: acps-backlog"
    - path: "skills/acps-architecture/SKILL.md"
      provides: "Architecture skill"
      contains: "name: acps-architecture"
    - path: "skills/acps-project-roadmap/SKILL.md"
      provides: "Project roadmap skill"
      contains: "name: acps-project-roadmap"
    - path: "skills/acps-branch/SKILL.md"
      provides: "Branch skill"
      contains: "name: acps-branch"
    - path: "skills/acps-discuss/SKILL.md"
      provides: "Discuss skill"
      contains: "name: acps-discuss"
    - path: "skills/acps-spec/SKILL.md"
      provides: "Spec skill"
      contains: "name: acps-spec"
    - path: "skills/acps-plan/SKILL.md"
      provides: "Plan skill"
      contains: "name: acps-plan"
    - path: "skills/acps-execute/SKILL.md"
      provides: "Execute skill"
      contains: "name: acps-execute"
    - path: "skills/acps-homologate/SKILL.md"
      provides: "Homologate skill"
      contains: "name: acps-homologate"
  key_links:
    - from: "package.json scripts.lint"
      to: "skills/**/*.md"
      via: "markdownlint-cli2 glob extended to include skills"
      pattern: "skills/**/*.md"
---

<objective>
Extend the lint script to cover all skill Markdown files, run the full lint gate, verify all 11 skill directories exist with the correct file structure, and present the complete skill set to the user for review.

Purpose: Quality gate for Phase 3. Skills are the deliverable — they must pass lint and have the correct structural pattern before the phase is marked complete.

Output: Updated package.json (lint script extended), human-verified skill completeness.
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md

<interfaces>
<!-- Current lint script (from package.json) -->
```json
"lint": "markdownlint-cli2 \"docs/**/*.md\" \"templates/**/*.md\" \"*.md\" \"!CHANGELOG.md\""
```

<!-- Target lint script (add skills/**/*.md) -->
```json
"lint": "markdownlint-cli2 \"docs/**/*.md\" \"templates/**/*.md\" \"skills/**/*.md\" \"*.md\" \"!CHANGELOG.md\""
```

<!-- Expected skills directory structure -->
skills/
  acps-init/SKILL.md + workflow.md
  acps-vision/SKILL.md + workflow.md
  acps-backlog/SKILL.md + workflow.md
  acps-architecture/SKILL.md + workflow.md
  acps-project-roadmap/SKILL.md + workflow.md
  acps-branch/SKILL.md + workflow.md
  acps-discuss/SKILL.md + workflow.md
  acps-spec/SKILL.md + workflow.md
  acps-plan/SKILL.md + workflow.md
  acps-execute/SKILL.md + workflow.md
  acps-homologate/SKILL.md + workflow.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend lint script to cover skills/ and run full lint gate</name>
  <files>package.json</files>

  <read_first>
    - /Users/danielvm/Sites/agentic-cps-method/package.json — current lint script to extend
    - /Users/danielvm/Sites/agentic-cps-method/.markdownlint.yaml — linting rules (if exists) to understand what SKILL.md/workflow.md must comply with
  </read_first>

  <action>
1. Read `package.json`. Find the `scripts.lint` field.

2. Update the lint script to add `"skills/**/*.md"` glob:

Current:
```json
"lint": "markdownlint-cli2 \"docs/**/*.md\" \"templates/**/*.md\" \"*.md\" \"!CHANGELOG.md\""
```

Updated:
```json
"lint": "markdownlint-cli2 \"docs/**/*.md\" \"templates/**/*.md\" \"skills/**/*.md\" \"*.md\" \"!CHANGELOG.md\""
```

3. Write the updated `package.json` (preserve all other fields exactly).

4. Run the lint command to verify all skill files pass:
```bash
npm run lint
```

5. If lint fails on any skill file:
   - Read the failing file
   - Fix the markdownlint violations (common issues: missing blank lines around headings, trailing spaces, inconsistent ATX headings)
   - Re-run until `npm run lint` exits 0

6. Verify all 11 skill directories exist with exactly 2 files each:
```bash
for skill in acps-init acps-vision acps-backlog acps-architecture acps-project-roadmap acps-branch acps-discuss acps-spec acps-plan acps-execute acps-homologate; do
  ls skills/$skill/
done
```

7. Run structural spot-checks:
```bash
grep -l "name: acps-" skills/*/SKILL.md | wc -l   # should be 11
grep -l "<workflow>" skills/*/workflow.md | wc -l  # should be 11
```
  </action>

  <verify>
    <automated>npm run lint && grep -c "skills/\*\*" /Users/danielvm/Sites/agentic-cps-method/package.json</automated>
  </verify>

  <acceptance_criteria>
    - `package.json` scripts.lint contains `"skills/**/*.md"`
    - `npm run lint` exits 0 (all Markdown files pass)
    - `ls skills/` lists exactly 11 directories: acps-architecture, acps-backlog, acps-branch, acps-discuss, acps-execute, acps-homologate, acps-init, acps-plan, acps-project-roadmap, acps-spec, acps-vision
    - Each skills/ subdirectory contains exactly SKILL.md and workflow.md (no extra files)
    - `grep -l "name: acps-" skills/*/SKILL.md | wc -l` outputs 11
    - `grep -l "<workflow>" skills/*/workflow.md | wc -l` outputs 11
  </acceptance_criteria>

  <done>package.json lint script extended to cover skills/**/*.md. npm run lint exits 0. All 11 skill directories verified to exist with correct 2-file structure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All 11 acps skills have been created across Plans 01–06:

Setup Phase Skills:
- skills/acps-init/ — creates .planning/ structure + .acps-config.json
- skills/acps-vision/ — CPS Ch.4 vision interview → vision.md
- skills/acps-backlog/ — CPS Ch.10 epic+iteration definition → backlog.md
- skills/acps-architecture/ — CPS Ch.14 architecture interview → architecture.md
- skills/acps-project-roadmap/ — CPS Ch.12 BCP/FP estimation → roadmap.md

GSD Loop Skills:
- skills/acps-branch/ — semantic branch creation, naming enforcement
- skills/acps-discuss/ — discuss loop, captures locked decisions
- skills/acps-spec/ — ambiguity scoring (gate ≤ 0.20), AC writing
- skills/acps-plan/ — task breakdown with BCP estimates
- skills/acps-execute/ — implementation + sessions[] telemetry append
- skills/acps-homologate/ — interactive AC walkthrough, totals computation, failure routing

The lint gate has passed (npm run lint exits 0).
  </what-built>

  <how-to-verify>
1. Run the structural check:
   ```
   npm run lint
   ls skills/
   ```
   Expected: lint exits 0, ls shows 11 directories.

2. Review acps-init (the foundation):
   - Open `skills/acps-init/SKILL.md` — should have 3 lines: `---`, `name: acps-init`, `description: ...`, `---`, blank, `Follow the instructions in ./workflow.md.`
   - Open `skills/acps-init/workflow.md` — should have 5 XML steps, references .acps-config.json and epics/, iterations/, specs/

3. Review acps-spec (most complex):
   - Open `skills/acps-spec/workflow.md` — should show 6 steps
   - Look for: `Ambiguity score: {score} > 0.20 threshold` (exact failure format)
   - Look for: `override` option
   - Look for: `## Test` section write

4. Review acps-homologate (most complex):
   - Open `skills/acps-homologate/workflow.md` — should show 5 steps
   - Look for: `1-pass, 2-fail` format (all ACs at once per D-12)
   - Look for: `totals:` block computation from sessions[]
   - Look for: `status: homologated` and `status: failed`
   - Look for: `acps-bug-fix` failure routing

5. Spot-check one loop skill of your choice (acps-branch, acps-discuss, acps-plan, or acps-execute).
  </how-to-verify>

  <resume-signal>Type "approved" if skills look correct, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI→skills | markdownlint runs on skills/**/*.md in CI — malformed Markdown would surface here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | skill file content | accept | Skill files are static Markdown; lint gate enforces structural validity; human checkpoint confirms correctness |
</threat_model>

<verification>
- `npm run lint` exits 0
- `ls skills/ | wc -l` outputs 11
- `grep -l "Follow the instructions in ./workflow.md" skills/*/SKILL.md | wc -l` outputs 11
- `grep -l "<workflow>" skills/*/workflow.md | wc -l` outputs 11
- `grep "skills/\*\*" package.json` exits 0
</verification>

<success_criteria>
- npm run lint passes with skills/**/*.md included (no markdownlint violations)
- All 11 skill directories exist, each with exactly SKILL.md and workflow.md
- Every SKILL.md has frontmatter (name + description) and workflow delegate line
- Every workflow.md has `<workflow>` root element
- Human reviewer has approved the skill structure and content
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-and-loop-skills/03-07-SUMMARY.md`
</output>
