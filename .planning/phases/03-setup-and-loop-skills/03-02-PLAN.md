---
phase: 03-setup-and-loop-skills
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - skills/acps-vision/SKILL.md
  - skills/acps-vision/workflow.md
  - skills/acps-backlog/SKILL.md
  - skills/acps-backlog/workflow.md
autonomous: true
requirements:
  - SETUP-02
  - SETUP-03

must_haves:
  truths:
    - "skills/acps-vision/ exists with SKILL.md and workflow.md"
    - "skills/acps-backlog/ exists with SKILL.md and workflow.md"
    - "acps-vision workflow reads .acps-config.json for planningDir and writes {planningDir}/vision.md"
    - "acps-backlog workflow reads .acps-config.json for planningDir and writes {planningDir}/backlog.md"
    - "Both workflows reference CPS chapter discipline (Ch.4 for vision, Ch.10 for backlog)"
    - "Both workflows use full XML step format with <step>, <action>, <check if>, <output> elements"
  artifacts:
    - path: "skills/acps-vision/SKILL.md"
      provides: "Vision skill entry point"
      contains: "name: acps-vision"
    - path: "skills/acps-vision/workflow.md"
      provides: "Vision creation workflow — CPS Ch.4"
      contains: "<workflow>"
    - path: "skills/acps-backlog/SKILL.md"
      provides: "Backlog skill entry point"
      contains: "name: acps-backlog"
    - path: "skills/acps-backlog/workflow.md"
      provides: "Backlog creation workflow — CPS Ch.10"
      contains: "<workflow>"
  key_links:
    - from: "skills/acps-vision/workflow.md"
      to: "{planningDir}/vision.md"
      via: "write action reading planningDir from .acps-config.json"
      pattern: "vision.md"
    - from: "skills/acps-backlog/workflow.md"
      to: "{planningDir}/backlog.md"
      via: "write action reading planningDir from .acps-config.json"
      pattern: "backlog.md"
---

<objective>
Create two Setup Phase skills: `acps-vision` (CPS Ch.4 — guides vision creation, writes vision.md) and `acps-backlog` (CPS Ch.10 — guides epic + iteration creation, writes backlog.md). Both skills read `.acps-config.json` for the planning directory path.

Purpose: After acps-init, the user needs to capture project vision and define the work breakdown (epics + iterations). These are the first two substantive CPS artifacts.

Output: 4 files total — SKILL.md + workflow.md for each of acps-vision and acps-backlog.
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md

<interfaces>
<!-- Canonical SKILL.md pattern -->
```markdown
---
name: <skill-name>
description: '<one-line description>'
---

Follow the instructions in ./workflow.md.
```

<!-- .acps-config.json read pattern used by every skill -->
```
1. Read .acps-config.json from project root
2. Extract planningDir value
3. If file missing: default planningDir = ".planning"
```

<!-- Templates available for skills to reference -->
<!-- templates/phases/vision.md — vision template (Phase 2 output) -->
<!-- templates/phases/backlog.md — backlog template (Phase 2 output) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create acps-vision skill (SKILL.md + workflow.md)</name>
  <files>skills/acps-vision/SKILL.md, skills/acps-vision/workflow.md</files>

  <read_first>
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/SKILL.md — SKILL.md pattern to mirror
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/workflow.md — XML step structure pattern
    - /Users/danielvm/Sites/agentic-cps-method/templates/phases/vision.md — vision template the workflow should populate
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md — SETUP-02 exact responsibilities
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md — D-05, D-06 (config reading), D-01–D-03 (file format)
  </read_first>

  <action>
Create `skills/acps-vision/SKILL.md`:

```markdown
---
name: acps-vision
description: 'Guides creation of the project vision document following CPS Chapter 4 discipline. Asks structured questions to extract purpose, target users, value proposition, and success metrics, then writes {planningDir}/vision.md.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-vision/workflow.md` with full XML workflow. The workflow must:

1. Read `.acps-config.json` (default to `.planning` if missing) per D-05/D-06
2. Check if vision.md already exists (warn but allow overwrite)
3. Interview the user using CPS Ch.4 questions: project name, problem being solved, target users, value proposition, differentiation, success metrics, non-goals
4. Compose the vision.md content using the answers
5. Write `{planningDir}/vision.md`
6. Report completion with next-step guidance

Structure:
```markdown
# acps-vision Workflow

**CPS Reference:** CPS Chapter 4 — Vision

## Context

`acps-vision` facilitates a structured vision workshop following CPS Chapter 4 discipline. It guides the user through the key questions that define the project's purpose, target audience, and success criteria, then writes a clean `vision.md` document.

---

<workflow>

<step n="1" goal="Load project configuration">
  <action>Look for `.acps-config.json` in the project root</action>
  <check if=".acps-config.json exists">
    <action>Read `.acps-config.json` and extract `planningDir` value</action>
  </check>
  <check if=".acps-config.json is missing">
    <action>Set planningDir to `.planning` (silent default per D-06)</action>
  </check>
</step>

<step n="2" goal="Check for existing vision">
  <action>Check if `{planningDir}/vision.md` already exists</action>
  <check if="vision.md exists">
    <output>A vision.md already exists at `{planningDir}/vision.md`.
Continuing will overwrite it. Type "overwrite" to proceed or "cancel" to stop.</output>
    <check if="user types 'cancel'">
      <action>HALT — existing vision preserved</action>
    </check>
  </check>
</step>

<step n="3" goal="Run CPS Chapter 4 vision interview">
  <output>**Vision Workshop — CPS Chapter 4**

I'll guide you through the key questions that define your project vision. Answer each question — the more specific, the better.
  </output>
  <action>Ask: "1. What is the project name?"</action>
  <action>Ask: "2. What problem does this project solve? (1-3 sentences)"</action>
  <action>Ask: "3. Who are the primary target users or customers?"</action>
  <action>Ask: "4. What is the core value proposition — why will users choose this over alternatives?"</action>
  <action>Ask: "5. What makes this different from existing solutions?"</action>
  <action>Ask: "6. How will you measure success? (2-3 specific, measurable metrics)"</action>
  <action>Ask: "7. What is explicitly out of scope for v1?"</action>
  <action>Record all answers as: project_name, problem_statement, target_users, value_proposition, differentiation, success_metrics, non_goals</action>
</step>

<step n="4" goal="Write vision.md">
  <action>Compose `{planningDir}/vision.md` using the interview answers:

```markdown
# {project_name} — Vision

## Problem

{problem_statement}

## Target Users

{target_users}

## Value Proposition

{value_proposition}

## Differentiation

{differentiation}

## Success Metrics

{success_metrics — formatted as a bulleted list}

## Non-Goals (v1)

{non_goals — formatted as a bulleted list}
```
  </action>
  <action>Write the composed content to `{planningDir}/vision.md`</action>
</step>

<step n="5" goal="Report completion">
  <output>
**acps-vision complete.**

Vision written to: `{planningDir}/vision.md`

**Next steps:**
  1. Run `acps-backlog` to define epics and iterations
  2. Run `acps-architecture` to capture the architecture package
  3. Run `acps-project-roadmap` to estimate delivery
  </output>
</step>

</workflow>
```
  </action>

  <verify>
    <automated>grep -c "name: acps-vision" /Users/danielvm/Sites/agentic-cps-method/skills/acps-vision/SKILL.md && grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-vision/workflow.md && grep -c "vision.md" /Users/danielvm/Sites/agentic-cps-method/skills/acps-vision/workflow.md && grep -c "planningDir" /Users/danielvm/Sites/agentic-cps-method/skills/acps-vision/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - `skills/acps-vision/SKILL.md` exists and contains `name: acps-vision`
    - `skills/acps-vision/SKILL.md` contains `Follow the instructions in ./workflow.md.`
    - `skills/acps-vision/workflow.md` exists and contains `<workflow>`
    - `skills/acps-vision/workflow.md` contains `<step n="1"` through `<step n="5"` (5 steps)
    - `skills/acps-vision/workflow.md` contains `planningDir` (reads config)
    - `skills/acps-vision/workflow.md` contains `acps-config.json` (config file reference)
    - `skills/acps-vision/workflow.md` contains `vision.md` (output file)
    - `skills/acps-vision/workflow.md` contains `Chapter 4` or `Ch.4` (CPS reference)
    - `skills/acps-vision/workflow.md` contains `</workflow>` closing tag
  </acceptance_criteria>

  <done>acps-vision skill created: thin SKILL.md with delegate line, workflow.md with 5 XML steps covering config loading, existing-file check, Ch.4 interview (7 questions), vision.md writing, and completion report.</done>
</task>

<task type="auto">
  <name>Task 2: Create acps-backlog skill (SKILL.md + workflow.md)</name>
  <files>skills/acps-backlog/SKILL.md, skills/acps-backlog/workflow.md</files>

  <read_first>
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/SKILL.md — SKILL.md pattern
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/workflow.md — XML step structure
    - /Users/danielvm/Sites/agentic-cps-method/templates/specs/epic.md — epic format backlog references
    - /Users/danielvm/Sites/agentic-cps-method/templates/phases/backlog.md — backlog template to populate
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md — SETUP-03 exact responsibilities
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md — D-05, D-06
  </read_first>

  <action>
Create `skills/acps-backlog/SKILL.md`:

```markdown
---
name: acps-backlog
description: 'Guides creation of the project backlog following CPS Chapter 10 discipline. Facilitates epic definition and iteration planning, writing {planningDir}/backlog.md with all epics and their story outlines.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-backlog/workflow.md` with full XML workflow. The workflow must:

1. Read `.acps-config.json` (default to `.planning` if missing) per D-05/D-06
2. Read `{planningDir}/vision.md` if it exists (for context)
3. Guide epic definition: for each epic, collect ID, title, description, goal, estimated BCP
4. For each epic, collect initial story outline (story titles and brief descriptions)
5. Guide iteration planning: how many iterations, which epics per iteration
6. Write `{planningDir}/backlog.md`
7. Report completion

Structure:
```markdown
# acps-backlog Workflow

**CPS Reference:** CPS Chapter 10 — Backlog

## Context

`acps-backlog` facilitates backlog construction following CPS Chapter 10 discipline. It guides the definition of epics (business capability clusters) and their breakdown into stories, then assigns epics to iterations for phased delivery.

---

<workflow>

<step n="1" goal="Load project configuration">
  <action>Look for `.acps-config.json` in the project root</action>
  <check if=".acps-config.json exists">
    <action>Read `.acps-config.json` and extract `planningDir` value</action>
  </check>
  <check if=".acps-config.json is missing">
    <action>Set planningDir to `.planning` (silent default per D-06)</action>
  </check>
  <check if="`{planningDir}/vision.md` exists">
    <action>Read `{planningDir}/vision.md` to provide context during epic definition</action>
  </check>
</step>

<step n="2" goal="Define epics — CPS Chapter 10">
  <output>**Backlog Workshop — CPS Chapter 10: Epic Definition**

An epic is a business capability cluster — a meaningful unit of value delivery. Let's define your epics.
  </output>
  <action>Ask: "How many epics will this project have? (Start with a number — you can add more later)"</action>
  <action>For each epic (repeat until all epics defined):
    - Ask: "Epic {N} title?"
    - Ask: "Epic {N} goal — what business value does this deliver?"
    - Ask: "Epic {N} stories — list the stories as brief titles (one per line)"
    - Ask: "Epic {N} BCP estimate — how many Business Complexity Points? (rough order of magnitude)"
    - Record as: id=EP-{N}, title, goal, stories[], bcp_estimate
  </action>
</step>

<step n="3" goal="Plan iterations">
  <output>**Iteration Planning — CPS Chapter 10**

Now assign epics to iterations. Each iteration delivers one or more epics.
  </output>
  <action>Ask: "How many iterations do you plan?"</action>
  <action>For each iteration (repeat until all iterations defined):
    - Ask: "Iteration {N} — which epics does it contain? (list epic IDs, e.g., EP-01, EP-02)"
    - Ask: "Iteration {N} — any iteration-specific goal or focus?"
    - Record as: id=ITER-{N}, epics[], goal
  </action>
</step>

<step n="4" goal="Write backlog.md">
  <action>Compose `{planningDir}/backlog.md`:

```markdown
# Project Backlog

## Epics

{for each epic:}
### {id}: {title}

**Goal:** {goal}
**BCP Estimate:** {bcp_estimate}

**Stories:**
{for each story in stories[]:}
- {story title}

---
{end for}

## Iterations

{for each iteration:}
### Iteration {id}

**Goal:** {goal}
**Epics:** {epics — comma-separated IDs}

---
{end for}
```
  </action>
  <action>Write the composed content to `{planningDir}/backlog.md`</action>
</step>

<step n="5" goal="Report completion">
  <output>
**acps-backlog complete.**

Backlog written to: `{planningDir}/backlog.md`
Epics defined: {count of epics}
Iterations planned: {count of iterations}

**Next steps:**
  1. Run `acps-architecture` to capture the architecture package
  2. Run `acps-project-roadmap` to apply BCP/FP estimation to the roadmap
  3. When iteration work begins: run `acps-branch` → `acps-discuss` → `acps-spec` → `acps-plan` → `acps-execute` → `acps-homologate`
  </output>
</step>

</workflow>
```
  </action>

  <verify>
    <automated>grep -c "name: acps-backlog" /Users/danielvm/Sites/agentic-cps-method/skills/acps-backlog/SKILL.md && grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-backlog/workflow.md && grep -c "backlog.md" /Users/danielvm/Sites/agentic-cps-method/skills/acps-backlog/workflow.md && grep -c "planningDir" /Users/danielvm/Sites/agentic-cps-method/skills/acps-backlog/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - `skills/acps-backlog/SKILL.md` exists and contains `name: acps-backlog`
    - `skills/acps-backlog/SKILL.md` contains `Follow the instructions in ./workflow.md.`
    - `skills/acps-backlog/workflow.md` exists and contains `<workflow>`
    - `skills/acps-backlog/workflow.md` contains `<step n="1"` through `<step n="5"` (5 steps)
    - `skills/acps-backlog/workflow.md` contains `planningDir` (config reading)
    - `skills/acps-backlog/workflow.md` contains `acps-config.json`
    - `skills/acps-backlog/workflow.md` contains `backlog.md` (output file)
    - `skills/acps-backlog/workflow.md` contains `Chapter 10` or `Ch.10` (CPS reference)
    - `skills/acps-backlog/workflow.md` contains `epic` and `iteration` (key CPS concepts)
    - `skills/acps-backlog/workflow.md` contains `</workflow>` closing tag
  </acceptance_criteria>

  <done>acps-backlog skill created: thin SKILL.md with delegate line, workflow.md with 5 XML steps covering config load, epic definition (Ch.10), iteration planning, backlog.md writing, and completion report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill→filesystem | Skills read .acps-config.json and write Markdown files to planningDir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | vision.md / backlog.md | accept | User-owned planning files; no secrets; low-value target |
| T-03-02-02 | Information Disclosure | planningDir contents | accept | Planning files are project documentation; user controls filesystem |
</threat_model>

<verification>
- `ls skills/acps-vision/` returns exactly `SKILL.md workflow.md`
- `ls skills/acps-backlog/` returns exactly `SKILL.md workflow.md`
- `grep "name: acps-vision" skills/acps-vision/SKILL.md` exits 0
- `grep "name: acps-backlog" skills/acps-backlog/SKILL.md` exits 0
- `grep "<workflow>" skills/acps-vision/workflow.md` exits 0
- `grep "<workflow>" skills/acps-backlog/workflow.md` exits 0
- `grep "vision.md" skills/acps-vision/workflow.md` exits 0
- `grep "backlog.md" skills/acps-backlog/workflow.md` exits 0
</verification>

<success_criteria>
- Both skills have exactly 2 files: SKILL.md (thin frontmatter + delegate) and workflow.md (full XML)
- acps-vision: 5 steps, reads config, runs Ch.4 interview (7 questions), writes vision.md
- acps-backlog: 5 steps, reads config, reads vision.md for context, runs Ch.10 epic+iteration definition, writes backlog.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-and-loop-skills/03-02-SUMMARY.md`
</output>
