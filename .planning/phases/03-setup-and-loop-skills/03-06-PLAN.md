---
phase: 03-setup-and-loop-skills
plan: 06
type: execute
wave: 3
depends_on:
  - 03-01
files_modified:
  - skills/acps-execute/SKILL.md
  - skills/acps-execute/workflow.md
  - skills/acps-homologate/SKILL.md
  - skills/acps-homologate/workflow.md
autonomous: true
requirements:
  - LOOP-05
  - LOOP-06

must_haves:
  truths:
    - "skills/acps-execute/ exists with SKILL.md and workflow.md"
    - "skills/acps-homologate/ exists with SKILL.md and workflow.md"
    - "acps-execute appends a correctly structured sessions[] entry to story YAML front-matter on completion"
    - "acps-homologate presents all ACs at once, collects per-AC pass/fail (e.g. '1-pass, 2-fail'), writes totals block"
    - "acps-homologate sets status: homologated when all ACs pass, status: failed when any fail"
    - "acps-homologate routes failures to acps-bug-fix without stopping mid-walkthrough"
  artifacts:
    - path: "skills/acps-execute/SKILL.md"
      provides: "Execute skill entry point"
      contains: "name: acps-execute"
    - path: "skills/acps-execute/workflow.md"
      provides: "Implementation workflow with sessions[] telemetry append"
      contains: "<workflow>"
    - path: "skills/acps-homologate/SKILL.md"
      provides: "Homologate skill entry point"
      contains: "name: acps-homologate"
    - path: "skills/acps-homologate/workflow.md"
      provides: "Interactive AC walkthrough + totals computation — CPS Ch.28"
      contains: "<workflow>"
  key_links:
    - from: "skills/acps-execute/workflow.md"
      to: "story YAML sessions[]"
      via: "appends session entry with duration_ms, tokens, model"
      pattern: "sessions:"
    - from: "skills/acps-homologate/workflow.md"
      to: "story YAML totals block"
      via: "computes from sessions[], writes totals"
      pattern: "totals:"
    - from: "skills/acps-homologate/workflow.md"
      to: "acps-bug-fix"
      via: "failure routing when any ACs fail"
      pattern: "acps-bug-fix"
---

<objective>
Create the final two GSD Loop skills: `acps-execute` (implementation workflow, appends session telemetry to story YAML front-matter) and `acps-homologate` (CPS Ch.28 — interactive AC walkthrough, computes totals block, routes failures to acps-bug-fix). These close the inner loop.

Purpose: acps-execute is where implementation happens, with telemetry automatically captured. acps-homologate is the quality gate — all ACs walked with the user before a story is marked done. These two skills complete the full CPS GSD loop.

Output: 4 files — SKILL.md + workflow.md for each of acps-execute and acps-homologate.
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md

<interfaces>
<!-- Story YAML front-matter schema — acps-execute appends to sessions[], acps-homologate writes totals -->
```yaml
---
story_id: ""
sessions: []
totals:
  sessions_count: 0
  total_duration_ms: 0
  tokens:
    input: 0
    output: 0
    total: 0
---
```

<!-- Session entry format appended by acps-execute -->
```yaml
- command: acps-execute
  started_at: "2026-04-19T10:00:00Z"
  finished_at: "2026-04-19T10:45:00Z"
  duration_ms: 2700000
  tokens:
    input: 45000
    output: 8000
    total: 53000
  model: "claude-opus-4"
```

<!-- D-12: All ACs at once, user marks by number: "1-pass, 2-fail, 3-pass" -->
<!-- D-13: totals = sum duration_ms, sum tokens from sessions[], count sessions -->
<!-- D-14: Any fail → status: failed, print N ACs failed → acps-bug-fix, complete full AC review first -->
<!-- D-15: All pass → status: homologated -->
<!-- Canonical SKILL.md pattern -->
```markdown
---
name: <skill-name>
description: '<description>'
---

Follow the instructions in ./workflow.md.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create acps-execute skill (SKILL.md + workflow.md)</name>
  <files>skills/acps-execute/SKILL.md, skills/acps-execute/workflow.md</files>

  <read_first>
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/SKILL.md — SKILL.md pattern
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/workflow.md — XML step structure and depth
    - /Users/danielvm/Sites/agentic-cps-method/templates/specs/story.md — YAML front-matter schema (sessions[], totals)
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md — LOOP-05 exact responsibilities
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md — D-05, D-06 (config), D-01–D-03 (file format)
  </read_first>

  <action>
Create `skills/acps-execute/SKILL.md`:

```markdown
---
name: acps-execute
description: 'Implements the current story task by task, following the Plan section. On completion, appends a session entry (command, started_at, finished_at, duration_ms, tokens, model) to the story YAML front-matter sessions[] array. Run after acps-plan.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-execute/workflow.md` with full XML workflow covering:

1. Read `.acps-config.json` for planningDir (silent default)
2. Load the story file — verify Plan section exists (gate on acps-plan having run)
3. Record session start: note start time
4. Walk through tasks in the Plan section one by one:
   - Display current task
   - Implement the task (agent does the work)
   - Mark task as done (`- [x]`)
   - Ask: "Task complete? (yes to continue, or describe blockers)"
5. After all tasks complete: record end time, prompt for token counts and model
6. Append session entry to story YAML front-matter sessions[]
7. Report completion with next step (acps-homologate)

Full content:

```markdown
# acps-execute Workflow

**CPS Reference:** CPS GSD Loop — Execute Phase

## Context

`acps-execute` drives implementation of the current story task by task, following the `## Plan` section. When execution completes, it records the session as a telemetry entry in the story's YAML front-matter `sessions[]` array. This data is later aggregated by `acps-homologate` to compute the `totals` block.

---

<workflow>

<step n="1" goal="Load story and verify plan gate passed">
  <action>Look for `.acps-config.json` in the project root</action>
  <check if=".acps-config.json exists">
    <action>Read `.acps-config.json` and extract `planningDir` value</action>
  </check>
  <check if=".acps-config.json is missing">
    <action>Set planningDir to `.planning` (silent default)</action>
  </check>
  <action>Ask: "Which story are we executing? Provide the path to the story file."</action>
  <check if="story file does not exist">
    <output>Story file not found. Check the path and try again.</output>
    <action>HALT</action>
  </check>
  <action>Read the story file. Extract: title, YAML front-matter (sessions[]), Plan section tasks, Test section ACs</action>
  <check if="## Plan section is missing or contains no tasks">
    <output>No implementation plan found in {story_file}.

Run `acps-plan` first to break the story into tasks before executing.</output>
    <action>HALT</action>
  </check>
  <action>Record session_start = current ISO 8601 timestamp</action>
</step>

<step n="2" goal="Execute tasks from Plan section">
  <output>**Execute Phase — {story title}**

Working through {task_count} tasks from the Plan section.
  </output>
  <action>For each unchecked task (`- [ ]`) in the ## Plan section (in order):
    - Display: "Task {N}/{total}: {task description}"
    - Implement the task: write code, create files, run commands as needed to satisfy the task
    - Mark the task done by updating `- [ ]` to `- [x]` in the story file
    - Display: "Task {N} complete."
    - Ask: "Continue to next task? (yes / describe blockers)"
    - Check if user describes blockers:
      - If blocked: record blocker, ask if user wants to skip this task or halt execution
      - If skipped: leave task unchecked with a note, continue
      - If halt requested: proceed to Step 3 (record partial session)
  </action>
</step>

<step n="3" goal="Record session end time and collect telemetry">
  <action>Record session_end = current ISO 8601 timestamp</action>
  <action>Calculate duration_ms = (session_end - session_start) in milliseconds</action>
  <action>Ask: "What model was used for this session? (e.g., claude-opus-4, claude-sonnet-4)"</action>
  <action>Ask: "Input tokens for this session? (check your IDE usage panel — enter 0 if unknown)"</action>
  <action>Ask: "Output tokens for this session? (enter 0 if unknown)"</action>
  <action>Calculate tokens_total = input_tokens + output_tokens</action>
</step>

<step n="4" goal="Append session to story YAML front-matter">
  <action>Read the story file's current YAML front-matter (the block between the opening --- and closing ---)</action>
  <action>Append a new entry to the `sessions:` array:

```yaml
- command: acps-execute
  started_at: "{session_start}"
  finished_at: "{session_end}"
  duration_ms: {duration_ms}
  tokens:
    input: {input_tokens}
    output: {output_tokens}
    total: {tokens_total}
  model: "{model}"
```
  </action>
  <action>Write the updated story file with the new session appended to the sessions[] array in the YAML front-matter</action>
</step>

<step n="5" goal="Report completion">
  <action>Count the number of checked tasks (`- [x]`) vs total tasks in ## Plan</action>
  <output>
**acps-execute complete.**

Tasks completed: {completed_count}/{total_count}
Session duration: {duration_ms}ms
Session appended to: {story_file_path}

{if completed_count < total_count:}
Warning: {total_count - completed_count} tasks remain uncomplete. Review before homologation.
{end if}

**Next step:** Run `acps-homologate` to walk all acceptance criteria with the user.
  </output>
</step>

</workflow>
```
  </action>

  <verify>
    <automated>grep -c "name: acps-execute" /Users/danielvm/Sites/agentic-cps-method/skills/acps-execute/SKILL.md && grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-execute/workflow.md && grep -c "sessions:" /Users/danielvm/Sites/agentic-cps-method/skills/acps-execute/workflow.md && grep -c "duration_ms" /Users/danielvm/Sites/agentic-cps-method/skills/acps-execute/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - `skills/acps-execute/SKILL.md` exists and contains `name: acps-execute`
    - `skills/acps-execute/SKILL.md` contains `Follow the instructions in ./workflow.md.`
    - `skills/acps-execute/workflow.md` exists and contains `<workflow>`
    - `skills/acps-execute/workflow.md` contains `<step n="1"` through `<step n="5"` (5 steps)
    - `skills/acps-execute/workflow.md` contains `sessions:` (YAML field being written)
    - `skills/acps-execute/workflow.md` contains `duration_ms` (telemetry field)
    - `skills/acps-execute/workflow.md` contains `started_at` (telemetry field)
    - `skills/acps-execute/workflow.md` contains `finished_at` (telemetry field)
    - `skills/acps-execute/workflow.md` contains `input:` and `output:` (token fields)
    - `skills/acps-execute/workflow.md` contains `model:` (model telemetry field)
    - `skills/acps-execute/workflow.md` contains `planningDir`
    - `skills/acps-execute/workflow.md` contains `acps-plan` (prerequisite gate reference)
    - `skills/acps-execute/workflow.md` contains `</workflow>` closing tag
  </acceptance_criteria>

  <done>acps-execute skill created: thin SKILL.md, workflow.md with 5 steps covering config+story load (gate on Plan section), task-by-task execution with blocker handling, session telemetry collection (start/end time, tokens, model), sessions[] append to YAML front-matter, completion report.</done>
</task>

<task type="auto">
  <name>Task 2: Create acps-homologate skill (SKILL.md + workflow.md)</name>
  <files>skills/acps-homologate/SKILL.md, skills/acps-homologate/workflow.md</files>

  <read_first>
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/SKILL.md — SKILL.md pattern
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/workflow.md — XML step structure and depth
    - /Users/danielvm/Sites/agentic-cps-method/templates/specs/story.md — full YAML front-matter schema (sessions[], totals)
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md — LOOP-06 exact responsibilities
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md — D-12, D-13, D-14, D-15 (all four homologate decisions)
  </read_first>

  <action>
Create `skills/acps-homologate/SKILL.md`:

```markdown
---
name: acps-homologate
description: 'Runs interactive UAT on the current story following CPS Chapter 28 discipline. Presents all acceptance criteria at once, collects per-AC pass/fail results, computes the totals block from sessions[], and marks the story as homologated or routes failures to acps-bug-fix.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-homologate/workflow.md` with full XML workflow implementing D-12, D-13, D-14, D-15 exactly:

- D-12: Present ALL ACs at once. User marks by number: "1-pass, 2-fail, 3-pass"
- D-13: Compute totals from sessions[] (sum duration_ms, sum tokens, count sessions)
- D-14: Any fail → status: failed, print "{N} ACs failed — run acps-bug-fix to file bugs and fix." Do NOT stop mid-walkthrough
- D-15: All pass → status: homologated

Full content:

```markdown
# acps-homologate Workflow

**CPS Reference:** CPS Chapter 28 — Continuous Homologation

## Context

`acps-homologate` runs the final quality gate of the CPS GSD loop. It presents all acceptance criteria simultaneously, collects user pass/fail verdicts, computes aggregate telemetry from the story's `sessions[]` array, and marks the story as `homologated` or `failed`. Failed stories route to `acps-bug-fix` for correction.

**AC walkthrough format (D-12):** All ACs presented at once. User marks by number: `1-pass, 2-fail, 3-pass`
**Totals computation (D-13):** Sum all sessions[] values — duration_ms, tokens.input, tokens.output, tokens.total; count sessions
**On failure (D-14):** Complete the full AC review first, then report failures and route to acps-bug-fix
**On full pass (D-15):** Set `status: homologated` in story YAML front-matter

---

<workflow>

<step n="1" goal="Load story and verify execution completed">
  <action>Look for `.acps-config.json` in the project root</action>
  <check if=".acps-config.json exists">
    <action>Read `.acps-config.json` and extract `planningDir` value</action>
  </check>
  <check if=".acps-config.json is missing">
    <action>Set planningDir to `.planning` (silent default)</action>
  </check>
  <action>Ask: "Which story are we homologating? Provide the path to the story file."</action>
  <check if="story file does not exist">
    <output>Story file not found. Check the path and try again.</output>
    <action>HALT</action>
  </check>
  <action>Read the full story file. Extract:
    - YAML front-matter: story_id, sessions[] array, current totals
    - ## Test section: all acceptance criteria (checkbox items)
    - Story title
  </action>
  <check if="## Test section is missing or empty">
    <output>No acceptance criteria found. Run `acps-spec` first, then `acps-execute` before homologating.</output>
    <action>HALT</action>
  </check>
  <check if="sessions[] array is empty">
    <output>No sessions recorded. Run `acps-execute` to implement the story and record a session before homologating.</output>
    <action>HALT</action>
  </check>
</step>

<step n="2" goal="Present all ACs at once for UAT (D-12)">
  <output>**Homologation — {story title}**

**CPS Chapter 28: User Acceptance Test**

Here are all acceptance criteria for this story. For each, verify the behavior in the running system, then mark your results using the format: `1-pass, 2-fail, 3-pass` (number corresponds to AC number below).

---

Acceptance Criteria:
{for each AC with its number:}
{N}. {AC description}
{end for}

---

Mark your results (e.g., "1-pass, 2-pass, 3-fail, 4-pass"):
  </output>
  <action>Wait for user's verdict string (format: "{N}-pass" or "{N}-fail" for each AC, comma-separated)</action>
  <action>Parse the verdict string: for each entry, record ac_number and result ("pass" or "fail")</action>
  <check if="any AC number in verdict is not in the AC list">
    <output>Unrecognized AC number in your verdict. Please re-enter with valid numbers ({min}–{max}).</output>
    <action>Re-display the ACs and ask for verdicts again</action>
  </check>
  <check if="not all AC numbers are covered in the verdict">
    <output>Missing verdict for AC(s): {missing_numbers}. Please provide a result for all ACs.</output>
    <action>Ask for the missing verdicts</action>
  </check>
</step>

<step n="3" goal="Compute totals from sessions[] (D-13)">
  <action>Iterate through all entries in the story YAML front-matter `sessions[]` array:
    - Sum all `duration_ms` values → total_duration_ms
    - Sum all `tokens.input` values → total_tokens_input
    - Sum all `tokens.output` values → total_tokens_output
    - Sum all `tokens.total` values → total_tokens_total
    - Count the number of sessions → sessions_count
  </action>
</step>

<step n="4" goal="Write totals block and set story status (D-14 / D-15)">
  <action>Count failed ACs from the verdicts collected in Step 2</action>
  <action>Update the story YAML front-matter:

1. Write the `totals` block:
```yaml
totals:
  sessions_count: {sessions_count}
  total_duration_ms: {total_duration_ms}
  tokens:
    input: {total_tokens_input}
    output: {total_tokens_output}
    total: {total_tokens_total}
```

2. Set story status:
   - If ALL ACs passed: add or update `status: homologated` in the YAML front-matter (D-15)
   - If ANY AC failed: add or update `status: failed` in the YAML front-matter (D-14)

3. Annotate the ## Test section with per-AC results:
   - For each passed AC: change `- [ ]` to `- [x]`
   - For each failed AC: keep as `- [ ]` and append ` ← FAILED`
  </action>
  <action>Write the updated story file</action>
</step>

<step n="5" goal="Report results and route failures (D-14 / D-15)">
  <action>Count total ACs, passed ACs, failed ACs</action>
  <check if="failed_count == 0">
    <output>
**acps-homologate complete — HOMOLOGATED.**

Story: {story_title}
All {total_count} ACs passed.
Status: homologated

Totals written:
- Sessions: {sessions_count}
- Total duration: {total_duration_ms}ms
- Total tokens: {total_tokens_total} ({total_tokens_input} in / {total_tokens_output} out)

**The story is complete. Merge your branch and celebrate.**
    </output>
  </check>
  <check if="failed_count > 0">
    <output>
**acps-homologate complete — FAILED.**

Story: {story_title}
{failed_count} AC(s) failed:
{for each failed AC:}
- AC {N}: {AC description}
{end for}

Status: failed

Totals written:
- Sessions: {sessions_count}
- Total duration: {total_duration_ms}ms
- Total tokens: {total_tokens_total}

{failed_count} ACs failed — run acps-bug-fix to file bugs and fix.
    </output>
  </check>
</step>

</workflow>
```
  </action>

  <verify>
    <automated>grep -c "name: acps-homologate" /Users/danielvm/Sites/agentic-cps-method/skills/acps-homologate/SKILL.md && grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-homologate/workflow.md && grep -c "totals:" /Users/danielvm/Sites/agentic-cps-method/skills/acps-homologate/workflow.md && grep -c "acps-bug-fix" /Users/danielvm/Sites/agentic-cps-method/skills/acps-homologate/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - `skills/acps-homologate/SKILL.md` exists and contains `name: acps-homologate`
    - `skills/acps-homologate/SKILL.md` contains `Follow the instructions in ./workflow.md.`
    - `skills/acps-homologate/workflow.md` exists and contains `<workflow>`
    - `skills/acps-homologate/workflow.md` contains `<step n="1"` through `<step n="5"` (5 steps)
    - `skills/acps-homologate/workflow.md` contains `1-pass, 2-fail` or `{N}-pass` (D-12 all-at-once format)
    - `skills/acps-homologate/workflow.md` contains `totals:` (D-13 totals block)
    - `skills/acps-homologate/workflow.md` contains `sessions_count:` (D-13 computation)
    - `skills/acps-homologate/workflow.md` contains `total_duration_ms` (D-13 field)
    - `skills/acps-homologate/workflow.md` contains `status: homologated` (D-15)
    - `skills/acps-homologate/workflow.md` contains `status: failed` (D-14)
    - `skills/acps-homologate/workflow.md` contains `acps-bug-fix` (D-14 failure routing)
    - `skills/acps-homologate/workflow.md` contains `ACs failed — run acps-bug-fix` (D-14 exact message format)
    - `skills/acps-homologate/workflow.md` contains `Chapter 28` (CPS reference)
    - `skills/acps-homologate/workflow.md` contains `</workflow>` closing tag
  </acceptance_criteria>

  <done>acps-homologate skill created: thin SKILL.md, workflow.md with 5 steps implementing D-12 (all ACs at once, numbered verdicts), D-13 (totals computed from sessions[]), D-14 (complete walkthrough before reporting failures, status: failed, "N ACs failed — run acps-bug-fix"), D-15 (status: homologated on full pass).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill→filesystem | Both skills read and write story YAML front-matter (sessions[], totals, status) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | sessions[] append | accept | Append-only operation; user owns their story files; no external data sources |
| T-03-06-02 | Tampering | homologate status write | accept | Status reflects user's explicit AC verdicts; user controlled; no automated override |
| T-03-06-03 | Information Disclosure | token counts in YAML | accept | Internal project telemetry; no PII; user owns the file |
</threat_model>

<verification>
- `ls skills/acps-execute/` returns exactly `SKILL.md workflow.md`
- `ls skills/acps-homologate/` returns exactly `SKILL.md workflow.md`
- `grep "name: acps-execute" skills/acps-execute/SKILL.md` exits 0
- `grep "name: acps-homologate" skills/acps-homologate/SKILL.md` exits 0
- `grep "sessions:" skills/acps-execute/workflow.md` exits 0
- `grep "duration_ms" skills/acps-execute/workflow.md` exits 0
- `grep "totals:" skills/acps-homologate/workflow.md` exits 0
- `grep "status: homologated" skills/acps-homologate/workflow.md` exits 0
- `grep "acps-bug-fix" skills/acps-homologate/workflow.md` exits 0
</verification>

<success_criteria>
- Both skills have exactly 2 files: SKILL.md (thin) + workflow.md (full XML)
- acps-execute: 5 steps, gates on Plan section, task-by-task implementation, session telemetry (start/end/tokens/model) appended to sessions[] YAML array
- acps-homologate: 5 steps, all ACs at once (D-12), totals computed from sessions[] (D-13), complete walkthrough before failure report (D-14), status: homologated/failed (D-14/D-15), routes to acps-bug-fix on failure
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-and-loop-skills/03-06-SUMMARY.md`
</output>
