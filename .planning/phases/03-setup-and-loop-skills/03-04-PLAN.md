---
phase: 03-setup-and-loop-skills
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
files_modified:
  - skills/acps-branch/SKILL.md
  - skills/acps-branch/workflow.md
  - skills/acps-discuss/SKILL.md
  - skills/acps-discuss/workflow.md
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02

must_haves:
  truths:
    - "skills/acps-branch/ exists with SKILL.md and workflow.md"
    - "skills/acps-discuss/ exists with SKILL.md and workflow.md"
    - "acps-branch workflow enforces semantic branch naming convention before anything else runs"
    - "acps-branch creates the branch as the first concrete action, not as an afterthought"
    - "acps-discuss workflow runs a GSD-style discuss loop extracting CPS implementation decisions"
    - "acps-discuss captures locked decisions and open questions to a decisions log"
  artifacts:
    - path: "skills/acps-branch/SKILL.md"
      provides: "Branch skill entry point"
      contains: "name: acps-branch"
    - path: "skills/acps-branch/workflow.md"
      provides: "Semantic branch creation with naming enforcement"
      contains: "<workflow>"
    - path: "skills/acps-discuss/SKILL.md"
      provides: "Discuss skill entry point"
      contains: "name: acps-discuss"
    - path: "skills/acps-discuss/workflow.md"
      provides: "CPS discuss loop with decision extraction"
      contains: "<workflow>"
  key_links:
    - from: "skills/acps-branch/workflow.md"
      to: "git branch"
      via: "git checkout -b command"
      pattern: "git checkout"
    - from: "skills/acps-discuss/workflow.md"
      to: "locked decisions"
      via: "structured question loop"
      pattern: "decision"
---

<objective>
Create the first two GSD Loop skills: `acps-branch` (creates semantic branch, enforces naming convention — must run before any other loop skill) and `acps-discuss` (GSD-style discuss loop that extracts CPS implementation decisions from the story context).

Purpose: The GSD loop starts with branching (discipline: no work without a branch) and continues with a discuss phase to clarify what must be built before writing specs. These two skills establish the loop entry point.

Output: 4 files — SKILL.md + workflow.md for each of acps-branch and acps-discuss.
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md

<interfaces>
<!-- Canonical SKILL.md pattern -->
```markdown
---
name: <skill-name>
description: '<one-line description>'
---

Follow the instructions in ./workflow.md.
```

<!-- Branch naming convention (GSD discipline) -->
<!-- Pattern: {iteration}-{epic-id}-{story-slug} -->
<!-- Examples: iter-1/ep-m01-user-auth, iter-2/ep-m03-payment-flow -->

<!-- Config read pattern -->
```
1. Read .acps-config.json → planningDir
2. If missing → ".planning" (silent default)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create acps-branch skill (SKILL.md + workflow.md)</name>
  <files>skills/acps-branch/SKILL.md, skills/acps-branch/workflow.md</files>

  <read_first>
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/SKILL.md — SKILL.md pattern
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/workflow.md — XML step structure
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md — LOOP-01 exact responsibilities
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md — D-05, D-06, D-01–D-03
  </read_first>

  <action>
Create `skills/acps-branch/SKILL.md`:

```markdown
---
name: acps-branch
description: 'Creates a semantic git branch for the current iteration story following CPS GSD discipline. Always run first — no loop work starts without a branch. Enforces the naming convention: {iteration}/{epic-id}-{story-slug}.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-branch/workflow.md` with full XML workflow covering:

1. Verify git repo exists (halt if not in a git repo)
2. Check current branch is clean (warn if uncommitted changes)
3. Ask for iteration number, epic ID, and story slug
4. Enforce naming convention: `iter-{N}/{epic-id}-{story-slug}` (show format, validate input)
5. Run `git checkout -b {branch-name}` from the default branch
6. Confirm branch created, display next step

Full content:

```markdown
# acps-branch Workflow

**CPS Reference:** CPS GSD Loop — Branch First

## Context

`acps-branch` enforces the GSD discipline that all work starts with a semantic branch. No story implementation, discussion, or spec work begins until a properly named branch exists. This creates a clean audit trail and enables safe parallel work across stories.

**Branch naming convention:** `iter-{N}/{epic-id}-{story-slug}`
Examples: `iter-1/ep-m01-user-auth`, `iter-2/ep-m03-payment-api`

---

<workflow>

<step n="1" goal="Verify git repository">
  <action>Run `git rev-parse --is-inside-work-tree` to check if the current directory is inside a git repo</action>
  <check if="command fails (not a git repo)">
    <output>Not inside a git repository.

Initialize git first: `git init && git add . && git commit -m "chore: initial commit"`
Then re-run `acps-branch`.</output>
    <action>HALT</action>
  </check>
</step>

<step n="2" goal="Check working directory state">
  <action>Run `git status --porcelain` to check for uncommitted changes</action>
  <check if="output is non-empty (uncommitted changes exist)">
    <output>Warning: you have uncommitted changes in your working directory.

It is recommended to commit or stash changes before branching.
Type "continue" to branch anyway, or "cancel" to stash/commit first.</output>
    <check if="user types 'cancel'">
      <action>HALT — user will commit/stash changes first</action>
    </check>
  </check>
</step>

<step n="3" goal="Collect branch components">
  <output>**Branch Creation — GSD Discipline**

Branch naming convention: `iter-{N}/{epic-id}-{story-slug}`
Example: `iter-1/ep-m01-user-authentication`
  </output>
  <action>Ask: "Iteration number? (e.g., 1, 2, 3)"</action>
  <action>Ask: "Epic ID? (e.g., ep-m01, ep-m02 — use the ID from your backlog)"</action>
  <action>Ask: "Story slug? (short kebab-case description, e.g., user-auth, payment-api)"</action>
  <action>Compose branch_name = `iter-{iteration}/{epic-id}-{story-slug}`</action>
  <action>Normalize branch_name: lowercase, replace spaces with hyphens, remove special characters except hyphens and forward slashes</action>
</step>

<step n="4" goal="Validate and create branch">
  <output>Branch to create: `{branch_name}`</output>
  <action>Ask: "Confirm? (Enter to confirm, or type a correction)"</action>
  <check if="user provides a correction">
    <action>Update branch_name to the corrected value and re-display</action>
    <action>Ask for final confirmation again</action>
  </check>
  <action>Run `git checkout -b {branch_name}`</action>
  <check if="git command fails">
    <output>Branch creation failed. Error: {git error output}

Common causes:
- Branch `{branch_name}` already exists → use `git checkout {branch_name}` instead
- Invalid branch name → check for invalid characters</output>
    <action>HALT — display the git error for the user to resolve</action>
  </check>
</step>

<step n="5" goal="Report completion">
  <output>
**acps-branch complete.**

Branch created: `{branch_name}`
You are now on branch: `{branch_name}`

**Next steps (in order):**
  1. Run `acps-discuss` — refine the story context and capture implementation decisions
  2. Run `acps-spec` — write acceptance criteria (ambiguity gate ≤ 0.20)
  3. Run `acps-plan` — break the story into tasks with BCP estimates
  4. Run `acps-execute` — implement
  5. Run `acps-homologate` — walk all ACs with the user
  </output>
</step>

</workflow>
```
  </action>

  <verify>
    <automated>grep -c "name: acps-branch" /Users/danielvm/Sites/agentic-cps-method/skills/acps-branch/SKILL.md && grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-branch/workflow.md && grep -c "git checkout" /Users/danielvm/Sites/agentic-cps-method/skills/acps-branch/workflow.md && grep -c "iter-" /Users/danielvm/Sites/agentic-cps-method/skills/acps-branch/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - `skills/acps-branch/SKILL.md` exists and contains `name: acps-branch`
    - `skills/acps-branch/SKILL.md` contains `Follow the instructions in ./workflow.md.`
    - `skills/acps-branch/workflow.md` exists and contains `<workflow>`
    - `skills/acps-branch/workflow.md` contains `<step n="1"` through `<step n="5"` (5 steps)
    - `skills/acps-branch/workflow.md` contains `git checkout` (branch creation command)
    - `skills/acps-branch/workflow.md` contains `iter-` (branch naming convention)
    - `skills/acps-branch/workflow.md` contains `git rev-parse` (repo verification)
    - `skills/acps-branch/workflow.md` contains `git status` (clean state check)
    - `skills/acps-branch/workflow.md` contains `</workflow>` closing tag
  </acceptance_criteria>

  <done>acps-branch skill created: thin SKILL.md, workflow.md with 5 steps covering git repo check, clean state warning, branch component collection, naming convention enforcement and branch creation, and completion report with full GSD loop next steps.</done>
</task>

<task type="auto">
  <name>Task 2: Create acps-discuss skill (SKILL.md + workflow.md)</name>
  <files>skills/acps-discuss/SKILL.md, skills/acps-discuss/workflow.md</files>

  <read_first>
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/SKILL.md — SKILL.md pattern
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/workflow.md — XML step structure and depth
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md — LOOP-02 exact responsibilities
    - /Users/danielvm/Sites/agentic-cps-method/templates/specs/story.md — story file format the discuss phase reads for context
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md — D-05, D-06, D-01–D-03
  </read_first>

  <action>
Create `skills/acps-discuss/SKILL.md`:

```markdown
---
name: acps-discuss
description: 'Runs a GSD-style discuss loop on the current story, extracting CPS implementation decisions, clarifying requirements, and capturing locked decisions. Run after acps-branch, before acps-spec.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-discuss/workflow.md` with full XML workflow covering:

1. Read `.acps-config.json` for planningDir (silent default)
2. Ask for the story file path (or detect from current branch name)
3. Read the story file to understand context
4. Run the discuss loop: ask implementation questions until user signals "done"
   - For each topic: present the question, collect answer, classify as locked decision or open question
5. Summarize locked decisions and open questions
6. Append a `## Decisions` section to the story file with the captured decisions
7. Report completion

Full content:

```markdown
# acps-discuss Workflow

**CPS Reference:** CPS GSD Loop — Discuss Phase

## Context

`acps-discuss` runs a structured discussion loop to extract the implementation decisions a story needs before specs can be written. Inspired by GSD's `/gsd-discuss-phase`, it turns ambiguity into locked decisions and surfaces open questions for the user to resolve. The output is a `## Decisions` section appended to the story file.

---

<workflow>

<step n="1" goal="Load configuration and story context">
  <action>Look for `.acps-config.json` in the project root</action>
  <check if=".acps-config.json exists">
    <action>Read `.acps-config.json` and extract `planningDir` value</action>
  </check>
  <check if=".acps-config.json is missing">
    <action>Set planningDir to `.planning` (silent default)</action>
  </check>
  <action>Ask: "Which story are we discussing? Provide the path to the story file (e.g., {planningDir}/specs/ep-m01-user-auth.md)"</action>
  <check if="story file does not exist at the provided path">
    <output>Story file not found: {provided_path}

Check the path and try again. Story files are typically in `{planningDir}/specs/`.</output>
    <action>HALT</action>
  </check>
  <action>Read the story file and extract: story title, Overview section, Plan section (tasks), Test section (ACs if any)</action>
</step>

<step n="2" goal="Present story context and open discussion">
  <output>**Discuss Phase — {story title}**

Story context loaded. I've read the story and identified areas that need implementation decisions before we can write specs.

Let's work through the key questions. For each topic, I'll ask a question — you answer, and I'll capture it as a locked decision or open question.

Type "done" at any point to end the discussion and finalize decisions.
  </output>
</step>

<step n="3" goal="Run discuss loop — extract implementation decisions">
  <action>Analyze the story content and identify 3-7 implementation topics that need decisions. Consider:
    - Technology or library choices (if multiple options exist)
    - Data model or schema design choices
    - API design or contract decisions
    - UX or interaction behavior decisions
    - Error handling or edge case behavior
    - Integration approach or sequence
    - Performance or scaling considerations
  </action>
  <action>For each identified topic:
    - Present the question clearly: "Decision needed: {topic} — {specific question}"
    - Wait for user's answer
    - Ask: "Is this locked (finalized decision) or still open (needs more thought)?"
    - Record as: {topic, question, answer, status: "locked" | "open"}
  </action>
  <action>After each answer, ask: "Any other topics to cover? (Type the topic or 'done' to finish)"</action>
  <check if="user types 'done'">
    <action>Exit the discuss loop and proceed to Step 4</action>
  </check>
  <action>Continue loop: present any remaining topics from the initial list, then open the floor for user-driven topics</action>
</step>

<step n="4" goal="Summarize and confirm decisions">
  <output>**Discussion Summary**

Locked decisions ({count}):
{for each locked decision: - **{topic}:** {answer}}

Open questions ({count}):
{for each open question: - **{topic}:** {answer} — needs resolution before spec}
  </output>
  <action>Ask: "Does this capture everything correctly? (Type 'yes' to write to story file, or make corrections)"</action>
  <check if="user makes corrections">
    <action>Apply corrections to the decisions list</action>
    <action>Re-display the summary</action>
    <action>Ask for final confirmation</action>
  </check>
</step>

<step n="5" goal="Append decisions to story file">
  <action>Append a `## Decisions` section to the story file (after the last existing section):

```markdown
## Decisions

<!-- Captured by acps-discuss on {current_date} -->

### Locked

{for each locked decision:}
- **{topic}:** {answer}
{end for}

### Open Questions

{for each open question:}
- **{topic}:** {answer} — resolve before running acps-spec
{end for}
```
  </action>
  <action>Write the updated story file</action>
</step>

<step n="6" goal="Report completion">
  <output>
**acps-discuss complete.**

Decisions written to: {story_file_path}
Locked decisions: {count}
Open questions: {count}

{if open_questions > 0:}
Resolve the open questions above before running `acps-spec`.
{end if}

**Next step:** Run `acps-spec` to write acceptance criteria (ambiguity gate ≤ 0.20).
  </output>
</step>

</workflow>
```
  </action>

  <verify>
    <automated>grep -c "name: acps-discuss" /Users/danielvm/Sites/agentic-cps-method/skills/acps-discuss/SKILL.md && grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-discuss/workflow.md && grep -c "decision" /Users/danielvm/Sites/agentic-cps-method/skills/acps-discuss/workflow.md && grep -c "locked" /Users/danielvm/Sites/agentic-cps-method/skills/acps-discuss/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - `skills/acps-discuss/SKILL.md` exists and contains `name: acps-discuss`
    - `skills/acps-discuss/SKILL.md` contains `Follow the instructions in ./workflow.md.`
    - `skills/acps-discuss/workflow.md` exists and contains `<workflow>`
    - `skills/acps-discuss/workflow.md` contains `<step n="1"` through `<step n="6"` (6 steps)
    - `skills/acps-discuss/workflow.md` contains `planningDir`
    - `skills/acps-discuss/workflow.md` contains `locked` (decision classification)
    - `skills/acps-discuss/workflow.md` contains `decision` (core concept)
    - `skills/acps-discuss/workflow.md` contains `## Decisions` (section appended to story)
    - `skills/acps-discuss/workflow.md` contains `</workflow>` closing tag
  </acceptance_criteria>

  <done>acps-discuss skill created: thin SKILL.md, workflow.md with 6 steps covering config load, story reading, discuss loop (3-7 implementation topics), decision summary+confirmation, decisions section appended to story file, and completion report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill→git | acps-branch runs git commands in user's repo |
| skill→filesystem | acps-discuss reads and appends to user's story files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | story file appends | accept | User-owned files; append-only; no destructive writes; user reviews summary before write |
| T-03-04-02 | Tampering | git branch creation | accept | Requires explicit user confirmation of branch name before `git checkout -b` runs |
| T-03-04-03 | Elevation of Privilege | git commands | accept | Only `git rev-parse`, `git status`, `git checkout -b` — no destructive git operations |
</threat_model>

<verification>
- `ls skills/acps-branch/` returns exactly `SKILL.md workflow.md`
- `ls skills/acps-discuss/` returns exactly `SKILL.md workflow.md`
- `grep "name: acps-branch" skills/acps-branch/SKILL.md` exits 0
- `grep "name: acps-discuss" skills/acps-discuss/SKILL.md` exits 0
- `grep "<workflow>" skills/acps-branch/workflow.md` exits 0
- `grep "<workflow>" skills/acps-discuss/workflow.md` exits 0
- `grep "git checkout" skills/acps-branch/workflow.md` exits 0
- `grep "locked" skills/acps-discuss/workflow.md` exits 0
</verification>

<success_criteria>
- Both skills have exactly 2 files: SKILL.md (thin) + workflow.md (full XML)
- acps-branch: 5 steps, enforces git repo, clean state warning, `iter-{N}/{epic-id}-{story-slug}` naming, creates branch with explicit confirmation before `git checkout -b`
- acps-discuss: 6 steps, reads story file, runs discuss loop (3-7 topics), captures locked vs open decisions, appends `## Decisions` section to story file
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-and-loop-skills/03-04-SUMMARY.md`
</output>
