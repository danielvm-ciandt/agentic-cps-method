---
phase: 03-setup-and-loop-skills
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - skills/acps-architecture/SKILL.md
  - skills/acps-architecture/workflow.md
  - skills/acps-project-roadmap/SKILL.md
  - skills/acps-project-roadmap/workflow.md
autonomous: true
requirements:
  - SETUP-04
  - SETUP-05

must_haves:
  truths:
    - "skills/acps-architecture/ exists with SKILL.md and workflow.md"
    - "skills/acps-project-roadmap/ exists with SKILL.md and workflow.md"
    - "acps-architecture workflow guides architecture package creation and writes {planningDir}/architecture.md"
    - "acps-project-roadmap workflow performs BCP/FP estimation on all epics and writes {planningDir}/roadmap.md"
    - "Both skills read .acps-config.json for planningDir (default .planning if missing)"
    - "Both workflows use full XML step format"
  artifacts:
    - path: "skills/acps-architecture/SKILL.md"
      provides: "Architecture skill entry point"
      contains: "name: acps-architecture"
    - path: "skills/acps-architecture/workflow.md"
      provides: "Architecture package creation workflow — CPS Ch.14"
      contains: "<workflow>"
    - path: "skills/acps-project-roadmap/SKILL.md"
      provides: "Project roadmap skill entry point"
      contains: "name: acps-project-roadmap"
    - path: "skills/acps-project-roadmap/workflow.md"
      provides: "BCP/FP estimation and roadmap writing — CPS Ch.12"
      contains: "<workflow>"
  key_links:
    - from: "skills/acps-architecture/workflow.md"
      to: "{planningDir}/architecture.md"
      via: "write action"
      pattern: "architecture.md"
    - from: "skills/acps-project-roadmap/workflow.md"
      to: "{planningDir}/roadmap.md"
      via: "write action reading epics from backlog.md"
      pattern: "roadmap.md"
---

<objective>
Create two Setup Phase skills: `acps-architecture` (CPS Ch.14 — guides architecture package creation, writes architecture.md) and `acps-project-roadmap` (CPS Ch.12 — BCP/FP estimation on all epics, writes roadmap.md). Both run in parallel in Wave 2 alongside Plan 02.

Purpose: After backlog is defined, the user needs the architecture package (constraints, tech decisions, patterns) and a delivery roadmap with BCP/FP estimates. These complete the Setup Phase.

Output: 4 files — SKILL.md + workflow.md for each of acps-architecture and acps-project-roadmap.
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md

<interfaces>
<!-- Canonical SKILL.md pattern -->
```markdown
---
name: <skill-name>
description: '<one-line description>'
---

Follow the instructions in ./workflow.md.
```

<!-- Config read pattern (all skills follow this) -->
```
1. Read .acps-config.json → extract planningDir
2. If missing → planningDir = ".planning" (silent default)
```

<!-- templates available -->
<!-- templates/phases/architecture-package.md — architecture template (Phase 2) -->
<!-- templates/deliverables/roadmap.md — roadmap template (Phase 2) -->
<!-- templates/phases/backlog.md — backlog that roadmap reads for epic list -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create acps-architecture skill (SKILL.md + workflow.md)</name>
  <files>skills/acps-architecture/SKILL.md, skills/acps-architecture/workflow.md</files>

  <read_first>
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/SKILL.md — SKILL.md pattern
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/workflow.md — XML step structure
    - /Users/danielvm/Sites/agentic-cps-method/templates/phases/architecture-package.md — architecture template to populate
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md — SETUP-04 exact responsibilities
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md — D-05, D-06
  </read_first>

  <action>
Create `skills/acps-architecture/SKILL.md`:

```markdown
---
name: acps-architecture
description: 'Guides creation of the architecture package following CPS Chapter 14 discipline. Captures tech stack decisions, system boundaries, key patterns, and quality attributes, then writes {planningDir}/architecture.md.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-architecture/workflow.md` with full XML workflow covering:

1. Read `.acps-config.json` (silent default `.planning` if missing)
2. Read `{planningDir}/vision.md` and `{planningDir}/backlog.md` if they exist (for context)
3. Guide architecture interview using CPS Ch.14 structure:
   - Tech stack decisions (languages, frameworks, libraries)
   - System boundaries and external integrations
   - Key architectural patterns (MVC, microservices, event-driven, etc.)
   - Data model overview (entities, relationships)
   - Quality attributes (performance, security, scalability requirements)
   - Deployment and infrastructure choices
   - Key technical constraints and non-negotiables
4. Write `{planningDir}/architecture.md`
5. Report completion

Full content:

```markdown
# acps-architecture Workflow

**CPS Reference:** CPS Chapter 14 — Architecture Package

## Context

`acps-architecture` facilitates architecture package creation following CPS Chapter 14 discipline. It captures the foundational technical decisions that will guide all implementation work: stack choices, system boundaries, patterns, and quality constraints.

---

<workflow>

<step n="1" goal="Load project configuration and context">
  <action>Look for `.acps-config.json` in the project root</action>
  <check if=".acps-config.json exists">
    <action>Read `.acps-config.json` and extract `planningDir` value</action>
  </check>
  <check if=".acps-config.json is missing">
    <action>Set planningDir to `.planning` (silent default)</action>
  </check>
  <check if="`{planningDir}/vision.md` exists">
    <action>Read `{planningDir}/vision.md` for project context</action>
  </check>
  <check if="`{planningDir}/backlog.md` exists">
    <action>Read `{planningDir}/backlog.md` for epic context</action>
  </check>
</step>

<step n="2" goal="Check for existing architecture package">
  <action>Check if `{planningDir}/architecture.md` already exists</action>
  <check if="architecture.md exists">
    <output>An architecture.md already exists at `{planningDir}/architecture.md`.
Continuing will overwrite it. Type "overwrite" to proceed or "cancel" to stop.</output>
    <check if="user types 'cancel'">
      <action>HALT — existing architecture package preserved</action>
    </check>
  </check>
</step>

<step n="3" goal="Run CPS Chapter 14 architecture interview">
  <output>**Architecture Workshop — CPS Chapter 14**

Let's define the architecture package. These decisions will guide all implementation work.
  </output>
  <action>Ask: "1. What is the primary tech stack? (languages, frameworks, runtime)"</action>
  <action>Ask: "2. What are the system boundaries? List external services and integrations."</action>
  <action>Ask: "3. What is the primary architectural pattern? (e.g., monolith, microservices, event-driven, MVC)"</action>
  <action>Ask: "4. Describe the data model at a high level — key entities and their relationships."</action>
  <action>Ask: "5. What are the quality attribute requirements? (performance SLAs, security level, uptime, scalability targets)"</action>
  <action>Ask: "6. What are the deployment and infrastructure choices? (cloud provider, containerization, CI/CD approach)"</action>
  <action>Ask: "7. What are the hard technical constraints or non-negotiables? (legacy systems, compliance requirements, team skills)"</action>
  <action>Record all answers as: tech_stack, system_boundaries, arch_pattern, data_model, quality_attributes, deployment, constraints</action>
</step>

<step n="4" goal="Write architecture.md">
  <action>Compose `{planningDir}/architecture.md`:

```markdown
# Architecture Package

**CPS Reference:** Chapter 14

## Tech Stack

{tech_stack}

## System Boundaries

{system_boundaries}

## Architectural Pattern

{arch_pattern}

## Data Model

{data_model}

## Quality Attributes

{quality_attributes}

## Deployment & Infrastructure

{deployment}

## Constraints & Non-Negotiables

{constraints}
```
  </action>
  <action>Write the composed content to `{planningDir}/architecture.md`</action>
</step>

<step n="5" goal="Report completion">
  <output>
**acps-architecture complete.**

Architecture package written to: `{planningDir}/architecture.md`

**Next steps:**
  1. Run `acps-project-roadmap` to apply BCP/FP estimation to the roadmap
  2. When iteration work begins: run `acps-branch` to start the GSD loop
  </output>
</step>

</workflow>
```
  </action>

  <verify>
    <automated>grep -c "name: acps-architecture" /Users/danielvm/Sites/agentic-cps-method/skills/acps-architecture/SKILL.md && grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-architecture/workflow.md && grep -c "architecture.md" /Users/danielvm/Sites/agentic-cps-method/skills/acps-architecture/workflow.md && grep -c "planningDir" /Users/danielvm/Sites/agentic-cps-method/skills/acps-architecture/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - `skills/acps-architecture/SKILL.md` exists and contains `name: acps-architecture`
    - `skills/acps-architecture/SKILL.md` contains `Follow the instructions in ./workflow.md.`
    - `skills/acps-architecture/workflow.md` exists and contains `<workflow>`
    - `skills/acps-architecture/workflow.md` contains `<step n="1"` through `<step n="5"` (5 steps)
    - `skills/acps-architecture/workflow.md` contains `planningDir`
    - `skills/acps-architecture/workflow.md` contains `acps-config.json`
    - `skills/acps-architecture/workflow.md` contains `architecture.md` (output file)
    - `skills/acps-architecture/workflow.md` contains `Chapter 14` (CPS reference)
    - `skills/acps-architecture/workflow.md` contains `</workflow>` closing tag
  </acceptance_criteria>

  <done>acps-architecture skill created: thin SKILL.md, workflow.md with 5 steps covering config+context load, existing-file guard, Ch.14 interview (7 questions), architecture.md writing, and completion report.</done>
</task>

<task type="auto">
  <name>Task 2: Create acps-project-roadmap skill (SKILL.md + workflow.md)</name>
  <files>skills/acps-project-roadmap/SKILL.md, skills/acps-project-roadmap/workflow.md</files>

  <read_first>
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/SKILL.md — SKILL.md pattern
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/workflow.md — XML step structure
    - /Users/danielvm/Sites/agentic-cps-method/templates/deliverables/roadmap.md — roadmap template to populate
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md — SETUP-05 exact responsibilities (BCP/FP estimation)
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md — D-05, D-06
  </read_first>

  <action>
Create `skills/acps-project-roadmap/SKILL.md`:

```markdown
---
name: acps-project-roadmap
description: 'Performs BCP/FP estimation on all project epics following CPS Chapter 12 discipline and writes {planningDir}/roadmap.md with delivery timeline, milestones, and scope baseline.'
---

Follow the instructions in ./workflow.md.
```

Create `skills/acps-project-roadmap/workflow.md` with full XML workflow covering:

1. Read `.acps-config.json` (silent default `.planning` if missing)
2. Read `{planningDir}/backlog.md` for the epic list (required — halt if missing)
3. Guide BCP estimation for each epic:
   - Review the epic's story list
   - Collect or confirm BCP estimate per epic
   - Collect FP (Function Points) estimate if user wants both methods
4. Calculate totals and derive delivery estimate
5. Guide milestone definition
6. Write `{planningDir}/roadmap.md`
7. Report completion

Full content:

```markdown
# acps-project-roadmap Workflow

**CPS Reference:** CPS Chapter 12 — Project Roadmap & Estimation

## Context

`acps-project-roadmap` applies BCP/FP estimation to all project epics following CPS Chapter 12 discipline. It reads the backlog, guides estimation of each epic, then writes a delivery roadmap with milestones and scope baseline.

---

<workflow>

<step n="1" goal="Load project configuration and backlog">
  <action>Look for `.acps-config.json` in the project root</action>
  <check if=".acps-config.json exists">
    <action>Read `.acps-config.json` and extract `planningDir` value</action>
  </check>
  <check if=".acps-config.json is missing">
    <action>Set planningDir to `.planning` (silent default)</action>
  </check>
  <action>Look for `{planningDir}/backlog.md`</action>
  <check if="backlog.md does NOT exist">
    <output>backlog.md not found at `{planningDir}/backlog.md`.

Run `acps-backlog` first to define your epics and iterations, then return to `acps-project-roadmap`.</output>
    <action>HALT</action>
  </check>
  <action>Read `{planningDir}/backlog.md` and extract all epics (ID, title, BCP estimate, stories)</action>
</step>

<step n="2" goal="BCP/FP estimation per epic — CPS Chapter 12">
  <output>**Project Roadmap — CPS Chapter 12: Estimation**

I'll guide you through BCP estimation for each epic. BCP (Business Complexity Points) measures relative business value and complexity.
  </output>
  <action>Ask: "Do you want to use BCP only, or BCP + FP (Function Points)? (default: BCP only)"</action>
  <action>For each epic extracted from backlog.md:
    - Display: "Epic {ID}: {title} — {story count} stories, current BCP estimate: {bcp_estimate}"
    - Ask: "Confirm or update the BCP estimate for {ID}: {title}? (press Enter to keep {bcp_estimate})"
    - If user provides a new value: update bcp_estimate for this epic
    - If FP mode selected: Ask: "FP estimate for {ID}?" and record fp_estimate
  </action>
  <action>Calculate totals: total_bcp = sum of all epic bcp_estimates, total_fp = sum of all fp_estimates (if collected)</action>
</step>

<step n="3" goal="Define delivery milestones">
  <output>**Milestone Definition**

Now let's define delivery milestones based on the iteration plan in your backlog.
  </output>
  <action>Display the iteration plan from backlog.md (iteration IDs, their epics, epic BCP totals)</action>
  <action>Ask: "What is the target delivery date or velocity assumption? (e.g., '4 BCP points per sprint' or 'target date: 2025-Q3')"</action>
  <action>For each iteration:
    - Ask: "Milestone name for Iteration {N}? (e.g., 'MVP Launch', 'Phase 1 Complete')"
    - Ask: "Target date for Iteration {N}? (optional — leave blank if unknown)"
    - Record as: milestone_name, target_date, epics[], bcp_total
  </action>
</step>

<step n="4" goal="Write roadmap.md">
  <action>Compose `{planningDir}/roadmap.md`:

```markdown
# Project Roadmap

**CPS Reference:** Chapter 12
**Total BCP:** {total_bcp}
{if fp_mode:}**Total FP:** {total_fp}{end if}

## Epic Summary

| Epic | Title | BCP | FP | Stories |
|------|-------|-----|----|---------|
{for each epic: | {id} | {title} | {bcp} | {fp or "-"} | {story_count} |}

## Milestones

{for each milestone:}
### {milestone_name} — Iteration {N}

**Target:** {target_date or "TBD"}
**Epics:** {epic IDs}
**BCP:** {bcp_total}

---
{end for}

## Scope Baseline

Total BCP at project start: **{total_bcp}**
{if fp_mode:}Total FP at project start: **{total_fp}**{end if}

> This baseline is used by `acps-gate` and `acps-change-request` to track scope changes.
```
  </action>
  <action>Write the composed content to `{planningDir}/roadmap.md`</action>
</step>

<step n="5" goal="Report completion">
  <output>
**acps-project-roadmap complete.**

Roadmap written to: `{planningDir}/roadmap.md`
Total BCP: {total_bcp}
Milestones: {count}

**Setup Phase complete.** Your project is ready to enter the GSD Loop.

**Next steps (GSD Loop):**
  1. Run `acps-branch` — create the iteration branch
  2. Run `acps-discuss` — refine story context and decisions
  3. Run `acps-spec` — write acceptance criteria (ambiguity gate ≤ 0.20)
  4. Run `acps-plan` — break story into tasks with BCP estimates
  5. Run `acps-execute` — implement the story
  6. Run `acps-homologate` — walk all ACs with the user, mark story done
  </output>
</step>

</workflow>
```
  </action>

  <verify>
    <automated>grep -c "name: acps-project-roadmap" /Users/danielvm/Sites/agentic-cps-method/skills/acps-project-roadmap/SKILL.md && grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-project-roadmap/workflow.md && grep -c "roadmap.md" /Users/danielvm/Sites/agentic-cps-method/skills/acps-project-roadmap/workflow.md && grep -c "BCP" /Users/danielvm/Sites/agentic-cps-method/skills/acps-project-roadmap/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - `skills/acps-project-roadmap/SKILL.md` exists and contains `name: acps-project-roadmap`
    - `skills/acps-project-roadmap/SKILL.md` contains `Follow the instructions in ./workflow.md.`
    - `skills/acps-project-roadmap/workflow.md` exists and contains `<workflow>`
    - `skills/acps-project-roadmap/workflow.md` contains `<step n="1"` through `<step n="5"` (5 steps)
    - `skills/acps-project-roadmap/workflow.md` contains `planningDir`
    - `skills/acps-project-roadmap/workflow.md` contains `acps-config.json`
    - `skills/acps-project-roadmap/workflow.md` contains `roadmap.md` (output file)
    - `skills/acps-project-roadmap/workflow.md` contains `BCP` (estimation method)
    - `skills/acps-project-roadmap/workflow.md` contains `Chapter 12` (CPS reference)
    - `skills/acps-project-roadmap/workflow.md` contains `backlog.md` (reads epic list from here)
    - `skills/acps-project-roadmap/workflow.md` contains `</workflow>` closing tag
  </acceptance_criteria>

  <done>acps-project-roadmap skill created: thin SKILL.md, workflow.md with 5 steps covering config+backlog load (with HALT if backlog missing), BCP/FP estimation per epic (Ch.12), milestone definition, roadmap.md writing, and completion report with full GSD loop next-steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill→filesystem | Skills read backlog.md and write architecture.md/roadmap.md to planningDir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | architecture.md / roadmap.md | accept | User-owned planning files; no secrets; low-value target |
| T-03-03-02 | Information Disclosure | BCP estimates | accept | Estimations are internal planning data; user controls their project |
</threat_model>

<verification>
- `ls skills/acps-architecture/` returns exactly `SKILL.md workflow.md`
- `ls skills/acps-project-roadmap/` returns exactly `SKILL.md workflow.md`
- `grep "name: acps-architecture" skills/acps-architecture/SKILL.md` exits 0
- `grep "name: acps-project-roadmap" skills/acps-project-roadmap/SKILL.md` exits 0
- `grep "<workflow>" skills/acps-architecture/workflow.md` exits 0
- `grep "<workflow>" skills/acps-project-roadmap/workflow.md` exits 0
- `grep "architecture.md" skills/acps-architecture/workflow.md` exits 0
- `grep "roadmap.md" skills/acps-project-roadmap/workflow.md` exits 0
- `grep "BCP" skills/acps-project-roadmap/workflow.md` exits 0
</verification>

<success_criteria>
- Both skills have exactly 2 files: SKILL.md (thin) + workflow.md (full XML)
- acps-architecture: 5 steps, reads config+context, runs Ch.14 interview (7 questions), writes architecture.md
- acps-project-roadmap: 5 steps, reads config+backlog (halts if missing), BCP/FP estimation (Ch.12), milestone definition, writes roadmap.md with scope baseline
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-and-loop-skills/03-03-SUMMARY.md`
</output>
