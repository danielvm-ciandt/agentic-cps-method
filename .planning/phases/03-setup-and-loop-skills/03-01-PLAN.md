---
phase: 03-setup-and-loop-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/acps-init/SKILL.md
  - skills/acps-init/workflow.md
autonomous: true
requirements:
  - SETUP-01

must_haves:
  truths:
    - "skills/acps-init/ directory exists with exactly SKILL.md and workflow.md"
    - "SKILL.md has YAML frontmatter with name and description, then the workflow delegate line"
    - "workflow.md creates .planning/ with epics/, iterations/, specs/ subdirectories in the target project"
    - "workflow.md writes .acps-config.json with planningDir key to the project root"
    - "workflow.md uses <step> XML blocks with explicit <action>, <check if>, and <output> elements"
  artifacts:
    - path: "skills/acps-init/SKILL.md"
      provides: "Skill entry point with name and description frontmatter"
      contains: "name: acps-init"
    - path: "skills/acps-init/workflow.md"
      provides: "Full init workflow: directory creation + config writing"
      contains: "<workflow>"
  key_links:
    - from: "skills/acps-init/SKILL.md"
      to: "skills/acps-init/workflow.md"
      via: "delegate line"
      pattern: "Follow the instructions in ./workflow.md"
    - from: "skills/acps-init/workflow.md"
      to: ".acps-config.json"
      via: "write action in workflow step"
      pattern: "acps-config.json"
---

<objective>
Create the `acps-init` skill — the foundation skill that all others depend on. It creates the `.planning/` directory structure and writes `.acps-config.json` with the `planningDir` key that every subsequent skill reads.

Purpose: Every other skill reads `.acps-config.json` for `planningDir`. Without acps-init running first in a user's project, no other skill knows where to write output. This skill is the project bootstrap.

Output: `skills/acps-init/SKILL.md` + `skills/acps-init/workflow.md`
</objective>

<execution_context>
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/agentic-cps-method/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/danielvm/Sites/agentic-cps-method/.planning/ROADMAP.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md
@/Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md

<interfaces>
<!-- Canonical SKILL.md pattern — mirror exactly -->
<!-- Source: /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/SKILL.md -->

```markdown
---
name: bmad-create-story
description: 'Creates a dedicated story file with all the context the agent will need to implement it later. Use when the user says "create the next story" or "create story [story identifier]"'
---

Follow the instructions in ./workflow.md.
```

<!-- .acps-config.json schema (written by acps-init, read by all skills) -->
```json
{
  "planningDir": ".planning"
}
```

<!-- Story YAML front-matter schema (for reference — acps-execute/homologate write to this) -->
```yaml
---
story_id: ""
sessions: []
totals:
  sessions_count: 0
  total_duration_ms: 0
  tokens:
    input: 0
    output: 0
    total: 0
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skills/acps-init/SKILL.md</name>
  <files>skills/acps-init/SKILL.md</files>

  <read_first>
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/SKILL.md — canonical pattern to mirror exactly (2-line YAML frontmatter + delegate line)
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md — SETUP-01 description for the skill's description field
  </read_first>

  <action>
Create `skills/acps-init/SKILL.md` mirroring the lean-spec bmad-create-story pattern exactly:

```markdown
---
name: acps-init
description: 'Initializes a project for CPS methodology. Creates the .planning/ directory structure (epics/, iterations/, specs/) and writes .acps-config.json with the planningDir key. Run once at project start before any other acps skill.'
---

Follow the instructions in ./workflow.md.
```

File must contain: YAML frontmatter block with `name:` and `description:` only, then a blank line, then the exact delegate line `Follow the instructions in ./workflow.md.`
  </action>

  <verify>
    <automated>grep -c "name: acps-init" /Users/danielvm/Sites/agentic-cps-method/skills/acps-init/SKILL.md && grep -c "Follow the instructions in ./workflow.md" /Users/danielvm/Sites/agentic-cps-method/skills/acps-init/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - File `skills/acps-init/SKILL.md` exists
    - File contains exactly `name: acps-init`
    - File contains exactly `description:` with a non-empty string value
    - File contains exactly `Follow the instructions in ./workflow.md.`
    - File starts with `---` (YAML frontmatter open)
    - File has second `---` closing the frontmatter block
    - No other content besides frontmatter + delegate line
  </acceptance_criteria>

  <done>skills/acps-init/SKILL.md exists, has correct YAML frontmatter (name + description), and ends with the workflow delegate line.</done>
</task>

<task type="auto">
  <name>Task 2: Create skills/acps-init/workflow.md</name>
  <files>skills/acps-init/workflow.md</files>

  <read_first>
    - /Users/danielvm/Sites/lean-spec/.claude/skills/bmad-create-story/workflow.md — XML `<step>` block structure, `<action>`, `<check if>`, `<output>` element patterns
    - /Users/danielvm/Sites/agentic-cps-method/.planning/REQUIREMENTS.md — SETUP-01 exact responsibilities
    - /Users/danielvm/Sites/agentic-cps-method/.planning/phases/03-setup-and-loop-skills/03-CONTEXT.md — D-05, D-06, D-07 (config file writing), D-01–D-03 (file format rules)
  </read_first>

  <action>
Create `skills/acps-init/workflow.md` in full XML step-by-step format. The workflow must:

1. Check if `.acps-config.json` already exists to avoid overwriting an existing project init
2. Prompt user for the planning directory path (default: `.planning`)
3. Create the directory structure
4. Write `.acps-config.json`
5. Report completion

Full content:

```markdown
# acps-init Workflow

**CPS Reference:** CPS Setup Phase — Project Initialization

## Context

`acps-init` bootstraps a project for CPS methodology discipline. It creates the canonical `.planning/` directory structure and writes `.acps-config.json` so all other `acps` skills know where to write their output. Run once at project start.

---

<workflow>

<step n="1" goal="Check for existing initialization">
  <action>Look for `.acps-config.json` in the current project root</action>
  <check if=".acps-config.json already exists">
    <action>Read `.acps-config.json` and extract the `planningDir` value</action>
    <output>Project already initialized. Planning directory: {planningDir}

If you want to re-initialize, delete `.acps-config.json` first and re-run acps-init.</output>
    <action>HALT — no further action needed</action>
  </check>
</step>

<step n="2" goal="Determine planning directory">
  <action>Ask the user: "Where should the planning directory be created? (default: .planning)"</action>
  <check if="user provides a custom path">
    <action>Set planningDir to the user-provided path (strip trailing slash if present)</action>
  </check>
  <check if="user accepts default or provides no input">
    <action>Set planningDir to `.planning`</action>
  </check>
</step>

<step n="3" goal="Create directory structure">
  <action>Create the following directories in the project root (create parent directories as needed):
    - {planningDir}/
    - {planningDir}/epics/
    - {planningDir}/iterations/
    - {planningDir}/specs/
  </action>
  <action>In each subdirectory, create a `.gitkeep` file so empty directories are tracked by git</action>
</step>

<step n="4" goal="Write .acps-config.json">
  <action>Write `.acps-config.json` to the project root with the following content:
```json
{
  "planningDir": "{planningDir}"
}
```
Where `{planningDir}` is the value determined in Step 2.
  </action>
</step>

<step n="5" goal="Report completion">
  <output>
**acps-init complete.**

Planning directory created: `{planningDir}/`
Structure:
  - {planningDir}/epics/      ← epic specification files
  - {planningDir}/iterations/ ← iteration tracking files
  - {planningDir}/specs/      ← story specification files

Config written: `.acps-config.json` (planningDir: "{planningDir}")

**Next steps:**
  1. Run `acps-vision` to create your project vision
  2. Run `acps-backlog` to define epics and iterations
  3. Run `acps-architecture` to capture the architecture package
  4. Run `acps-project-roadmap` to estimate and plan delivery
  </output>
</step>

</workflow>
```
  </action>

  <verify>
    <automated>grep -c "<workflow>" /Users/danielvm/Sites/agentic-cps-method/skills/acps-init/workflow.md && grep -c '<step n="1"' /Users/danielvm/Sites/agentic-cps-method/skills/acps-init/workflow.md && grep -c "acps-config.json" /Users/danielvm/Sites/agentic-cps-method/skills/acps-init/workflow.md</automated>
  </verify>

  <acceptance_criteria>
    - File `skills/acps-init/workflow.md` exists
    - File contains `<workflow>` root element
    - File contains `<step n="1"` through `<step n="5"` (5 steps)
    - File contains `<action>` elements within steps
    - File contains `<check if=` conditionals
    - File contains `<output>` elements
    - File contains `acps-config.json` (the config file being written)
    - File contains `planningDir` key reference
    - File contains `epics/`, `iterations/`, `specs/` directory names
    - File contains `</workflow>` closing tag
  </acceptance_criteria>

  <done>skills/acps-init/workflow.md exists with full XML workflow: 5 steps covering config check, directory prompting, directory creation, config writing, and completion report. All three subdirectories (epics/, iterations/, specs/) explicitly named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill→filesystem | Skill writes .acps-config.json and creates directories in user's project root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | .acps-config.json | accept | Low-value target; user controls their own project root; no secrets stored |
| T-03-01-02 | Information Disclosure | planningDir path | accept | Path is user-supplied and stored in their own project; no PII involved |
</threat_model>

<verification>
- `ls skills/acps-init/` returns exactly `SKILL.md workflow.md`
- `grep "name: acps-init" skills/acps-init/SKILL.md` exits 0
- `grep "Follow the instructions in ./workflow.md" skills/acps-init/SKILL.md` exits 0
- `grep "<workflow>" skills/acps-init/workflow.md` exits 0
- `grep "acps-config.json" skills/acps-init/workflow.md` exits 0
- `grep "epics/" skills/acps-init/workflow.md` exits 0
</verification>

<success_criteria>
- `skills/acps-init/` contains exactly two files: SKILL.md and workflow.md
- SKILL.md: thin frontmatter (name, description) + delegate line only
- workflow.md: 5 XML steps, creates 3 subdirectories, writes .acps-config.json with planningDir key
- No extra files, no prose-only instructions, no hardcoded `.planning/` (uses prompt + default)
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-and-loop-skills/03-01-SUMMARY.md`
</output>
